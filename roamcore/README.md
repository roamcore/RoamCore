# roamcore/

Legacy/experimental RoamCore sources (pre-HA-only packaging).

This tree contains older structure such as:
- `Config/`
- `Frontend/`
- `roamcore-lxc/`

For current production work:
- Home Assistant integration + assets: `homeassistant/`
- UI JS shipped to HA: `homeassistant/www/roamcore/`
- Operator scripts: `scripts/`
