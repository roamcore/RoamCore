# Example secrets for RoamCore.
# Copy relevant keys into Home Assistant's /config/secrets.yaml.
# Never commit real secrets.

roamcore_traccar_admin_email: "admin@roamcore.local"
roamcore_traccar_admin_password: "CHANGE_ME"

# Optional (preferred): token-based auth for Traccar session creation
roamcore_traccar_user_token: "OPTIONAL_TOKEN"

