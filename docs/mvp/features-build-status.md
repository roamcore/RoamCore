# RoamCore MVP — Features Build Status

Last updated: 2026-03-31

This is an internal status page for the remaining MVP feature build-out.

## Shipped (repo)

- Weather + time contract sensors
  - `homeassistant/packages/roamcore_weather_time.yaml`

- Timezone override contract sensor (no HA restart required)
  - `sensor.rc_time_zone` via `input_text.rc_time_zone_override`

- Levelling contract (HA-only beta)
  - `homeassistant/packages/roamcore_level.yaml`
  - auto-maps common ESPHome pitch/roll sensors into stable `rc_level_*` entities

- Map view wiring
  - `dashboard/lovelace/storage/lovelace.roamcore.json` includes `/lovelace/roamcore/map`
  - `homeassistant/packages/roamcore_location.yaml` maps a configurable `device_tracker` → `rc_location_*`

- Trip Wrapped (MVP HTML export)
  - tool: `homeassistant/tools/trip_wrapped/`
  - HA wiring: `homeassistant/packages/roamcore_trip_wrapped.yaml`
  - output: `/local/roamcore/trip_wrapped/latest.html`

- OpenClaw JSON API (HA-native)
  - endpoint: `/api/roamcore/openclaw/summary`
  - docs: `docs/reference/openclaw-json-api.md`

- Traccar live map (embedded)
  - RoamCore Map page embeds Traccar add-on **web UI** via iframe (configurable).
  - Helper: `input_text.rc_traccar_ui_url`

- Remote access (vendor-neutral remote-access umbrella for HA — Tailscale + Cloudflare Tunnel + Nabu Casa HA Cloud + Wireguard, operator picks ONE path) (tier-b recipe connection)
  - tier-b manifest: `connections/remote-access/connection.yml` (networking category, beta status; reuse-first recipe over upstream HA core `tailscale` integration (Path A mesh VPN since 2022.x — exposes tailnet device status via `binary_sensor.tailscale_*` + `device_tracker.tailscale_*` entities) + HACS `cloudflared` add-on (Path B Cloudflare Tunnel — installs the `cloudflared` daemon that tunnels traffic through Cloudflare's edge to the HA server's local URL) + HA Core `cloud` integration (Path C Nabu Casa HA Cloud since 2022.x — exposes the remote URL via `sensor.home_assistant_cloud_remote` + the `cloud.remote_connect` / `cloud.remote_disconnect` services) + HACS `wireguard` add-on (Path D Wireguard self-hosted VPN — installs the Wireguard server in the HA server) + HA Companion app's `external_url` setting (the canonical OFF-LAN affordance since 2022.x) + a thin RoamCore path-routing wrapper; RoamCore does NOT maintain a custom remote-access engine; `install.hacs: true` + `install.config_flow: true` because the recipe depends on the HA core `tailscale` integration + the HACS `cloudflared` add-on + the HA Core `cloud` integration + the HACS `wireguard` add-on + the HA Companion app's `external_url` setting all expose a GUI flow since 2022.x — honest upstream truth, NOT a tier-a marker for RoamCore's tier; FOUR operator-pickable paths — Path A Tailscale mesh VPN (default for operators who want a secure mesh VPN without opening inbound ports + with MagicDNS hostname resolution; cross-references the Wave 2 #29 branch `feat/wave2-remote-access-tailscale` @ `0caa9c2` which shipped the Tailscale-specific contract at `homeassistant/packages/roamcore_remote_access.yaml`); Path B Cloudflare Tunnel no-inbound-ports (default for operators with a Cloudflare-managed domain + who want Cloudflare's edge caching + DDoS protection); Path C Nabu Casa HA Cloud official cloud relay (default for operators who want the HA Core official cloud relay + who do NOT want to manage a self-hosted VPN server + who are willing to pay for the subscription); Path D Wireguard self-hosted VPN (default for operators who prefer self-hosted VPN over managed services + who are comfortable with per-client key management))
  - recipe: `connections/remote-access/docs/recipe.md` (~1114-line howto: HA core `tailscale` integration install + HACS `cloudflared` add-on install + HA Core `cloud` integration install + HACS `wireguard` add-on install + Path A Tailscale mesh VPN wiring (7 steps: install the HA core `tailscale` integration OR the HACS Tailscale add-on + login to Tailscale + enable MagicDNS + verify HA is reachable via `https://<host>.ts.net` + add the operator's devices to the tailnet + configure the `binary_sensor.rc_remote_access_enabled` kill-switch + configure `select.rc_remote_access_path = tailscale`) + Path B Cloudflare Tunnel wiring (7 steps: create a Cloudflare account + add the operator's domain to Cloudflare + create a Cloudflare Tunnel pointing at the HA server's local URL + install the `cloudflared` daemon via the HACS `cloudflared` add-on + verify the tunnel is reachable + configure the kill-switch + configure `select.rc_remote_access_path = cloudflare_tunnel`) + Path C Nabu Casa HA Cloud wiring (5 steps: subscribe to Nabu Casa HA Cloud via the HA Cloud panel + enable remote access + verify the Nabu Casa remote URL + configure the kill-switch + configure `select.rc_remote_access_path = nabu_casa`) + Path D Wireguard self-hosted VPN wiring (8 steps: install the HACS `wireguard` add-on OR a manual Wireguard install + generate server keys + generate per-client keys + configure the Wireguard server interface + add the operator's devices as Wireguard peers + configure firewall rules + verify the VPN tunnel + configure the kill-switch + configure `select.rc_remote_access_path = wireguard`) + the 9 `rc_remote_access_*` contract tiles (1 binary_sensor operator kill-switch + 1 sensor remote-access URL + 1 binary_sensor active gate + 1 sensor active-path indicator + 1 sensor peer count + 1 sensor last-verified minutes ago + 1 binary_sensor hostname-resolvable gate + 1 button verify-now + 1 select operator-chosen path = 9 contract entities); FIVE MANDATORY §8 automations (§8.1 kill-switch ON enables remote access + §8.2 kill-switch OFF disables remote access + §8.3 auto-verify every 15 minutes + §8.4 notify on path switch + §8.5 Stealth-mode suppression via `select.rc_mode`); 6 §9 troubleshooting entries (remote access not reachable + hostname not resolving + Cloudflare Tunnel 1033/1034 errors + Nabu Casa remote URL blank + Wireguard handshake failing + path-switch leaves stale hostname in `sensor.rc_remote_access_url`); §10 privacy (no RoamCore-side telemetry; Tailscale logs are operator-owned via the Tailscale admin console + Cloudflare Tunnel logs are operator-owned via the Cloudflare dashboard + Nabu Casa logs are operator-owned via the HA Cloud panel + Wireguard logs are operator-owned on the HA server); §11 tier-a promotion outline (real remote-access bench on CI + RoamCore-owned operator-wired setup flow walking the operator through path selection + the FOUR upstream integrations + integration tests asserting the kill-switch + the active gate + the freshness gate + the path switch notification + the Stealth-mode suppression))
  - manifest-honesty smoke: `connections/remote-access/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_tier_b_without_tier_a_markers` defensive guard asserting tier=b + `wizard.one_tap=false` + `install.config_flow=true` honest because the UPSTREAM HA core `tailscale` integration + the HACS `cloudflared` add-on + the HACS `wireguard` add-on + the HA Core `cloud` integration + the HA Companion app's `external_url` setting all expose a GUI flow since 2022.x + `install.hacs=true` because remote access optionally depends on the HACS `cloudflared` add-on + the HACS `wireguard` add-on + the HACS Tailscale add-on + NO RoamCore-owned `config_flow.py` + DOMAIN=`remote_access` + the description explicitly documents the reuse-first strategy over the upstream HA core `tailscale` integration + the links.official list includes the HA core `tailscale` integration upstream doc URL; the `test_dashboard_tiles_follow_rc_naming` defensive guard asserting 9 vendor-neutral `rc_remote_access_*` tiles (NOT `rc_tailscale_*` and NOT `rc_cloudflare_*` and NOT `rc_nabu_casa_*` and NOT `rc_wireguard_*`) + forbidden_substrings covers vendor + hardware + protocol + integration names including `tailscale`, `ts_net`, `ts.net`, `cloudflare`, `cloudflared`, `cf-tunnel`, `cf_tunnel`, `argo_tunnel`, `tunnel_id`, `nabu_casa`, `hass-cloud`, `ha_cloud`, `ha_cloud_url`, `ha-cloud-url`, `wireguard`, `wg0`, `wg_`, `magicdns`, `magic_dns`, `nfcpy`, `nfc_`, `nfc.`, `hacs`, `hass`, `ha_integration`, `ha_companion`, `mqtt`, `esphome`, `esp32`, `esp8266`, `traccar`, `wican`, `obd`, `frigate`, `homeassistant`, `device_tracker`, `set_location`, `update_entity`, `zone_`, `zone.`, `binary_sensor_`, `sensor_`, `switch`, `input_boolean`, `input_select`, `input_number`, `input_datetime`, `input_text`, `template:`, `automation`, `scene`, `script`; the `test_status_reflects_no_real_remote_access_engine` defensive guard asserting status=beta + 5 tier_warnings present (`no_native_remote_access_engine` + `recipe_depends_on_operator_choosing_one_remote_access_path` + `requires_operator_wiring_upstream_integration_first` + `magic_dns_cloudflare_dns_or_nabu_casa_account_setup_is_operator_owned` + `ha_server_firewall_must_permit_chosen_path_port_range`); the `test_automations_are_documented` defensive guard asserting the FIVE §8 automations documented (kill-switch ON + kill-switch OFF + auto-verify every 15 minutes + notify on path switch + Stealth-mode suppression) + 4 safety tiles wired (`binary_sensor.rc_remote_access_enabled` + `button.rc_remote_access_verify_now` + `select.rc_remote_access_path` + `binary_sensor.rc_remote_access_active`) + cross-references to HA core `tailscale` integration + home-assistant.io/integrations/tailscale + cloudflared + cloud integration + wireguard + HA Companion app + mode/automation-builder recipe's `select.rc_mode` tile + Wave 2 #29 `feat/wave2-remote-access-tailscale` @ `0caa9c2` + approach-lights Wave 3 #52)
  - 9 contract entities all `rc_remote_access_*`: 4 sensor tiles (`sensor.rc_remote_access_url` / `sensor.rc_remote_access_active_path` / `sensor.rc_remote_access_peer_count` / `sensor.rc_remote_access_last_verified_minutes_ago`) + 3 binary_sensor tiles (`binary_sensor.rc_remote_access_enabled` / `binary_sensor.rc_remote_access_active` / `binary_sensor.rc_remote_access_hostname_resolvable`) + 1 button tile (`button.rc_remote_access_verify_now`) + 1 select tile (`select.rc_remote_access_path`) (per docs/reference/rc-entity-naming.md §remote_access subsystem — the `remote_access` subsystem is OWNED by this slice; the `remote_access` subsystem was added by this slice to the Allowed subsystems list in `docs/reference/rc-entity-naming.md`; the spec-required tile IDs legitimately contain `enabled`, `url`, `active`, `path`, `peer`, `count`, `last`, `verified`, `minutes`, `ago`, `hostname`, `resolvable`, `verify`, `now`, `operator`, `chosen` in the suffix to describe what the tile is for — these are the generic-noun tolerance list per spec; the forbidden substrings target the vendor-name / hardware-name / protocol-name / integration-name absolute-forbidden set only; the tile prefix is `rc_remote_access_*` — NOT `rc_tailscale_*` and NOT `rc_cloudflare_*` and NOT `rc_nabu_casa_*` and NOT `rc_wireguard_*` — the umbrella uses the `rc_remote_access_*` prefix because `remote_access` is the canonical vendor-neutral remote-access subsystem that covers all FOUR operator-pickable paths)
  - vendor-neutrality strictly enforced: NO `tailscale`, `ts_net`, `ts.net`, `cloudflare`, `cloudflared`, `cf-tunnel`, `cf_tunnel`, `argo_tunnel`, `tunnel_id`, `nabu_casa`, `hass-cloud`, `ha_cloud`, `ha_cloud_url`, `ha-cloud-url`, `wireguard`, `wg0`, `wg_`, `magicdns`, `magic_dns`, `nfcpy`, `nfc_`, `nfc.`, `hacs`, `hass`, `ha_integration`, `ha_companion`, `mqtt`, `esphome`, `esp32`, `esp8266`, `traccar`, `wican`, `obd`, `frigate`, `homeassistant`, `device_tracker`, `set_location`, `update_entity`, `zone_`, `zone.`, `binary_sensor_`, `sensor_`, `switch`, `input_boolean`, `input_select`, `input_number`, `input_datetime`, `input_text`, `template:`, `automation`, `scene`, `script` appears in any rc_* tile id BEYOND the `rc_remote_access_*` subsystem prefix
  - legacy tier-b catalog pages (`docs/catalog/remote-access/index.md` + `docs/catalog/remote-access/tailscale.md`) now carry supersession banners pointing at the new connection folder
  - cross-reference to HA core `tailscale` integration: §3 Path A Tailscale mesh VPN wiring uses the HA core `tailscale` integration (since 2022.x — exposes tailnet device status via `binary_sensor.tailscale_*` + `device_tracker.tailscale_*` entities)
  - cross-reference to HACS `cloudflared` add-on: §4 Path B Cloudflare Tunnel wiring uses the HACS `cloudflared` add-on (HACS — installs the `cloudflared` daemon on the HA server itself)
  - cross-reference to HA Core `cloud` integration: §5 Path C Nabu Casa HA Cloud wiring uses the HA Core `cloud` integration (since 2022.x — exposes the remote URL via `sensor.home_assistant_cloud_remote` + the `cloud.remote_connect` / `cloud.remote_disconnect` services)
  - cross-reference to HACS `wireguard` add-on: §6 Path D Wireguard self-hosted VPN wiring uses the HACS `wireguard` add-on (HACS — installs the Wireguard server in the HA server)
  - cross-reference to HA Companion app external_url setting: the HA Companion app's `external_url` setting (since 2022.x) is the canonical OFF-LAN affordance that points the operator's phone at the chosen remote-access URL when the operator is OFF-LAN
  - cross-reference to mode/automation-builder recipe: §8.5 Stealth-mode suppression automation uses the mode/automation-builder recipe's `select.rc_mode` tile (the source of truth for the Stealth-mode suppression; Wave 2 #23)
  - cross-reference to approach-lights (optional): the recipe's §12 cross-references the approach-lights Wave 3 #52 connection for the canonical ON-LAN-only lighting scene that Stealth-mode suppresses
  - cross-reference to nfc-tags (optional): the recipe's §12 cross-references the NFC tags Wave 3 #57 connection for the optional "tag-toggle-kill-switch" affordance that uses NFC scan events to toggle the `binary_sensor.rc_remote_access_enabled` kill-switch
  - cross-reference to Wave 2 #29 Tailscale contract: the recipe's §3 cross-references the Wave 2 #29 branch (`feat/wave2-remote-access-tailscale` @ `0caa9c2`) which shipped the Tailscale-specific contract at `homeassistant/packages/roamcore_remote_access.yaml` (the Wave 2 contract layer for Path A only; this slice LIFTS that contract into the `connections/` pipeline + ADDS the broader vendor-neutral contract layer + ADDS Cloudflare Tunnel + Nabu Casa + Wireguard as alternative paths)
  - PR #62

## Next steps (needs HAOS setup / UI wiring)

1) **Setup Wizard dashboard**
   - Add a Lovelace dashboard YAML for setup flow.
   - Wire stubs to OpenWrt API + Victron connect UI.

2) **Traccar install + integration in HAOS**
   - Install Traccar add-on (or point to external).
   - Configure HA Traccar integration so `device_tracker.*` exists.
   - Set `input_text.rc_location_tracker_entity` to the correct entity.

3) **Trip stats (rc_trip_*) from real Traccar data**
   - MVP still uses mocks for distance/time/stops.
   - Implement: odometer-based + utility_meter or periodic report pulls.

4) **HACS packaging (planned)**
   - Publish a HACS integration to install RoamCore from the HA UI.
   - Auto-create dashboard + resources.
