# RoamCore MVP — Features Build Status

Last updated: 2026-03-31

This is an internal status page for the remaining MVP feature build-out.

## Shipped (repo)

- Weather + time contract sensors
  - `homeassistant/packages/roamcore_weather_time.yaml`

- Timezone override contract sensor (no HA restart required)
  - `sensor.rc_time_zone` via `input_text.rc_time_zone_override`

- Levelling contract (HA-only beta)
  - `homeassistant/packages/roamcore_level.yaml`
  - auto-maps common ESPHome pitch/roll sensors into stable `rc_level_*` entities

- Map view wiring
  - `dashboard/lovelace/storage/lovelace.roamcore.json` includes `/lovelace/roamcore/map`
  - `homeassistant/packages/roamcore_location.yaml` maps a configurable `device_tracker` → `rc_location_*`

- Trip Wrapped (MVP HTML export)
  - tool: `homeassistant/tools/trip_wrapped/`
  - HA wiring: `homeassistant/packages/roamcore_trip_wrapped.yaml`
  - output: `/local/roamcore/trip_wrapped/latest.html`

- OpenClaw JSON API (HA-native)
  - endpoint: `/api/roamcore/openclaw/summary`
  - docs: `docs/reference/openclaw-json-api.md`

- Traccar live map (embedded)
  - RoamCore Map page embeds Traccar add-on **web UI** via iframe (configurable).
  - Helper: `input_text.rc_traccar_ui_url`

- Peplink (multi-WAN router for van internet) (tier-b connection manifest)
  - tier-b manifest: `connections/peplink/connection.yml` (networking category, beta status; covers HA core `snmp` Path A for single-router operators + Peplink InControl 2 REST API via the community HACS `hass-incontrol2` integration Path B for fleet operators)
  - recipe: `connections/peplink/docs/recipe.md` (~870-line howto: Path A SNMP wiring + Path B InControl 2 wiring + force-failover affordance via Path A or Path B + WAN-priority `select` + mode-aware multi-WAN preference (cellular in Travel/Boost, Starlink in Home/Shore) + 4 automations + 8 troubleshooting entries + privacy + tier-a promotion outline)
  - manifest-honesty smoke: `connections/peplink/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only`)
  - contract entities: `binary_sensor.rc_net_peplink_reachable`, `sensor.rc_net_peplink_wan1_state`, `sensor.rc_net_peplink_wan2_state`, `sensor.rc_net_peplink_active_wan`, `sensor.rc_net_peplink_wan_failover_count_24h`, `sensor.rc_net_peplink_wan_health_score`, `sensor.rc_net_peplink_uptime_hours`, `sensor.rc_net_peplink_public_ip`, `button.rc_net_peplink_refresh_now`, `button.rc_net_peplink_force_failover`, `select.rc_net_peplink_wan_priority` (all `rc_net_peplink_*` per docs/reference/rc-entity-naming.md §net subsystem)
  - legacy tier-c catalog page (`docs/catalog/networking/peplink.md`) now carries a supersession banner pointing at the new connection folder
  - PR #N

- Music Assistant (multi-room van audio) (tier-b connection manifest)
  - tier-b manifest: `connections/music-assistant/connection.yml` (media category, beta status; honest HACS-only install — `install.hacs: true` + `install.config_flow: true` because the UPSTREAM HACS `music_assistant` integration has a config_flow since 2023; HACS-only because HA core does NOT include Music Assistant; auto-discover via zeroconf on the LAN; covers Path A HA add-on for HAOS installs + Path B external MA server via `ghcr.io/music-assistant/server` for fleet installs + non-HAOS hosts)
  - recipe: `connections/music-assistant/docs/recipe.md` (~960-line howto: HACS install + Path A HA add-on wiring + Path B external MA server wiring + provider configuration (Spotify OAuth / Apple Music dev token / TuneIn / local-files mount / Chromecast / AirPlay / Sonos receivers) + per-zone `rc_media_zone_*` template media_player aliases + now-playing trio + library counts + `pause_all` / `resume_last` button affordances + 6 automations (Stealth auto-pause / Travel motion-resume / Boost zone-default-volume / inverter-SOC power-aware pause / TTS-zone-pinning / remember-last-played) + 8 troubleshooting entries + privacy + tier-a promotion outline)
  - manifest-honesty smoke: `connections/music-assistant/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only`)
  - contract entities: `media_player.rc_media_zone_living`, `media_player.rc_media_zone_bed`, `media_player.rc_media_zone_outdoor`, `binary_sensor.rc_media_any_player_playing`, `sensor.rc_media_active_zone`, `sensor.rc_media_now_playing_title`, `sensor.rc_media_now_playing_artist`, `sensor.rc_media_now_playing_album`, `sensor.rc_media_library_artists_count`, `sensor.rc_media_library_albums_count`, `sensor.rc_media_library_tracks_count`, `button.rc_media_pause_all`, `button.rc_media_resume_last`, `select.rc_media_default_zone` (all `rc_media_*` per docs/reference/rc-entity-naming.md §media subsystem — added to the allowed subsystems list alongside this slice)
  - legacy tier-c catalog page (`docs/catalog/audio-media/music-assistant.md`) now carries a supersession banner pointing at the new connection folder
  - HACS-only install is honestly disclosed: MA is fetched from `music-assistant/hass-music-assistant` via HACS → Repositories → Add (HA core does NOT include Music Assistant)
  - PR #N
- Bluetooth / Wi-Fi presence (who's home?) (tier-b connection manifest)
  - tier-b manifest: `connections/bluetooth-wifi-presence/connection.yml` (presence category, beta status; recipe-over-upstream — no RoamCore-owned presence scanner; `install.config_flow: true` is honest because HA core `nmap_device_tracker` (Path B) + `asuswrt` / `unifi` / `mikrotik` (Path C) integrations expose a config_flow since 2022.x; `bluetooth_le_tracker` Path A is YAML-only (deprecated upstream but still functional); no HACS integration of our own is shipped — Paths A/B/C all use existing HA core integrations)
  - recipe: `connections/bluetooth-wifi-presence/docs/recipe.md` (~260-line howto: Path A `bluetooth_le_tracker` YAML wiring + screensaver-sleep workaround via BLE beacon (Nut find3 / Apple AirTag) + Path B `nmap_device_tracker` / `ping` wiring + per-device template helper YAML + Path C `asuswrt` / `unifi` / `mikrotik` router-side wiring + HA `person` entity OR template helper translation into the contract layer + 10 `rc_presence_*` contract tiles + 6 automations (Stealth suppression / Bluetooth + Wi-Fi agreement rule (legacy catalog spec §7) / approach lighting on first arrival after dark / inverter+pump shutdown on all-away + shore disconnected + >15 min / only-driver-home dim interior to 10 % after dark / power-aware occupancy alert TTS to living zone) + 8 troubleshooting entries + privacy + tier-a promotion outline)
  - manifest-honesty smoke: `connections/bluetooth-wifi-presence/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only`)
  - contract entities: `device_tracker.rc_presence_person_alice`, `device_tracker.rc_presence_person_bob`, `binary_sensor.rc_presence_anyone_home`, `binary_sensor.rc_presence_only_driver_home`, `sensor.rc_presence_persons_home_count`, `sensor.rc_presence_last_arrival`, `sensor.rc_presence_last_departure`, `binary_sensor.rc_presence_all_away`, `button.rc_presence_refresh_now`, `select.rc_presence_occupied_threshold_minutes` (all `rc_presence_*` per docs/reference/rc-entity-naming.md §presence subsystem — added to the allowed subsystems list alongside this slice, mirroring how `media` was added by the Music Assistant slice)
  - vendor-neutrality strictly enforced: NO `bluetooth`, `bt`, `wifi`, `wlan`, `arp`, `nmap`, `ping`, `asuswrt`, `ubiquiti`, `unifi`, `mikrotik`, `iphone`, `android`, `pixel`, `galaxy` appears in any tile id BEYOND the subsystem prefix `rc_presence_*`
  - legacy tier-c catalog page (`docs/catalog/presence/bluetooth-wifi-presence.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to Music Assistant: the §7.6 "power-aware occupancy alert" automation sends TTS to `media_player.rc_media_zone_living` when shore is disconnected AND ≥2 people are home AND inverter SOC < 30 % — uses the Music Assistant `connections/music-assistant/` contract tile as the TTS target
  - cross-reference to Victron: the §7.4 inverter-shutdown automation + §7.6 power-aware alert both depend on `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` — uses the Victron `connections/victron/` contract tiles as the power source
  - PR #N
- Happijac bed lift (van bed up/down control) (tier-b connection manifest)
  - tier-b manifest: `connections/happijac/connection.yml` (bed_lift category, beta status; HA core `cover` template Path A ESPHome-friendly + Path B relay-friendly; `install.config_flow: true` is honest because the UPSTREAM HA core `cover` template integration + ESPHome `cover:` component expose a config_flow since 2022.x / 2023.x respectively; no RoamCore-owned wrapper / no native integration code is shipped — both paths recipe over upstream ESPHome or HA core `template` cover + 2× dry-contact relays + 2× limit microswitches + optional CT-clamp current sensor)
  - recipe: `connections/happijac/docs/recipe.md` (~360-line howto: Path A ESPHome YAML wiring for the 2× outputs (UP + DOWN relay coils) + 2× binary_sensor limit inputs (up_limit + down_limit, both with `delayed_off: 100ms` filter) + optional ADC current sensor for obstruction detection + the ESPHome `cover:` component declaring `cover.bed_lift`; Path B Shelly 1 / Shelly Plus 1 / Zooz ZEN17 / Aeotec Nano Switch pair + HA core `template:` cover wiring + the limit binary_sensor + the `current_based_obstruction_detection` block; 12 `rc_bed_lift_*` contract tiles (cover position + 2× limits + moving + position_pct + safety_ok + 3× button + obstruction_detected + low_voltage_lockout + mode select); 4 safety interlocks (limit-sanity aggregate / low-voltage lockout via `sensor.rc_power_battery_soc` from Victron / current-based obstruction detection / mode-aware lockouts Stealth + Sleep + Boost); 6 automations (Stealth auto-stop / Sleep 23:00 auto-lower / Boost disable-mode-aware-lockouts / low-voltage lockout when SOC < 20 % / obstruction detected → stop + alert via MA TTS / mode-aware gentle reminder when only-driver-home for >15 min); 8 troubleshooting entries (relay polarity / one limit stuck / both limits TRUE wiring fault / NC-NO mis-wire / obstruction false-positive tune current threshold / low-voltage lockout stuck after charging / Shelly not discovered mDNS IGMP / ESPHome device offline); §9 privacy (no telemetry beyond local limit microswitches + optional CT clamp; no Happijac cloud / no ESPHome cloud / no Shelly cloud / no Zooz / Aeotec cloud); §10 tier-a promotion outline (real Happijac + ESPHome + relay bench on CI + RoamCore-owned config_flow.py + integration tests asserting the 4 safety interlocks all flip + a 0→100% `cover.rc_bed_lift_position` change triggers the right tile updates))
  - manifest-honesty smoke: `connections/happijac/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_interlocks_are_documented` defensive guard for the future tier-a promotion's hard-enforced safety interlock asserts)
  - 12 contract entities all `rc_bed_lift_*`: `cover.rc_bed_lift_position`, `binary_sensor.rc_bed_lift_up_limit`, `binary_sensor.rc_bed_lift_down_limit`, `binary_sensor.rc_bed_lift_moving`, `sensor.rc_bed_lift_position_pct`, `binary_sensor.rc_bed_lift_safety_ok`, `button.rc_bed_lift_lift`, `button.rc_bed_lift_lower`, `button.rc_bed_lift_stop`, `binary_sensor.rc_bed_lift_obstruction_detected`, `binary_sensor.rc_bed_lift_low_voltage_lockout`, `select.rc_bed_lift_mode` (per docs/reference/rc-entity-naming.md §bed_lift subsystem — added to the allowed subsystems list alongside this slice, mirroring how `media` was added by the Music Assistant slice AND how `presence` was backfilled in the same edit since Wave 3 #42 promised it but didn't deliver)
  - vendor-neutrality strictly enforced: NO `happijac`, `happi`, `lc_`, `lci`, `bed`, `lift`, `actuator`, `esphome`, `shelly`, `zooz`, `aeotec`, `relay`, `cover_*`, `dry_contact` appears in any tile id BEYOND the `rc_bed_lift_*` subsystem prefix
  - legacy tier-c catalog page (`docs/catalog/bed-lift/happijac.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to Victron: §6.2 safety interlock for low-voltage lockout uses `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` from the Victron `connections/victron/` recipe
  - cross-reference to Bluetooth / Wi-Fi presence: §7.6 "if bed is in the up position AND presence says only-driver-home for >15 min AND mode is Sleep → gentle reminder to lower bed before sleeping" automation uses the bluetooth-wifi-presence `connections/bluetooth-wifi-presence/` contract tile (`binary_sensor.rc_presence_only_driver_home`) for the presence signal
  - PR #47
- Heated floors + engine pre-heat (cold-weather comfort) (tier-b connection manifest)
  - tier-b manifest: `connections/heated-floors/connection.yml` (hvac category, beta status; Path A smart thermostat Mysa / Shelly H&T / generic-Zigbee (operator already owns) + Path B HA core `generic_thermostat:` wrapping a temperature probe + a relay-driven heater + Path C optional engine pre-heat Webasto / Espar / Eberspächer / DIY coolant-loop via a relay or CAN bus gateway; `install.config_flow: true` is honest because the UPSTREAM HA core `generic_thermostat` integration exposes a GUI flow since 2022.x + the climate-domain vendor integrations (Path A) expose their own GUI flow; no RoamCore-owned wrapper / no native integration code is shipped — all three paths recipe over upstream vendor integrations or HA core `generic_thermostat` + `switch` + `sensor`)
  - recipe: `connections/heated-floors/docs/recipe.md` (~370-line howto: Path A smart thermostat wiring + the entity_id surfacing + the `set_hvac_mode` / `set_temperature` service calls + a `template:` sensor that derives the "Heating" / "Maintaining" / "Off" states from the climate's `hvac_action` attribute; Path B HA core `generic_thermostat:` YAML wrapping a `sensor.interior_temp` + a `switch.heater_relay` with `min_temp` / `max_temp` / `cold_tolerance` / `hot_tolerance` / `keep_alive` settings; Path C optional engine pre-heat via a relay or CAN bus gateway + a `binary_sensor.engine_preheat_active` template that derives "is the engine preheat actually producing heat" from the cabin temp trend (when the switch is on AND cabin temp is rising over a 5-minute window → active); 13 `rc_hvac_*` contract tiles (climate thermostat + floor current temp + interior temp + 3× state binary_sensors (heating / maintaining / off) + heater switch + numeric setpoint + mode select + low-voltage lockout + engine preheat switch + active binary_sensor + runtime tracker); 5 safety interlocks (low-voltage lockout via `sensor.rc_power_battery_soc` from Victron / shore-power-aware throttling heat aggressively when on shore + conserve when off / mode-aware Stealth + Sleep + Boost / presence-aware pre-warm when operator phone reconnects / frost-protection automation); 6 automations ("Warm up" scene + shore-power-aware + mode-aware Stealth + Sleep + Boost + low-voltage lockout when SOC < 20 % + presence-aware pre-warm + engine pre-heat schedule auto-start 06:30 weekdays); 9 troubleshooting entries (floor heater not turning on relay polarity + coil voltage, heater stuck on relay welded, temperature probe reading -40 °C probe disconnected, setpoint not sticking template race keep_alive, floor heater cycling rapidly cold_tolerance too tight, engine preheat switch toggles but engine doesn't warm CAN bus gateway not connected, engine preheat brief but runtime tracker incremented sensor debounce, low-voltage lockout stuck after charging cross-check Victron SOC, generic_thermostat min_temp clamp missing template render error); §9 privacy (no telemetry beyond local 1-Wire / Zigbee / Wi-Fi temperature probes; no vendor cloud / no engine preheat cloud); §10 tier-a promotion outline (real heated-floor + relay + temperature probe + optional engine preheat bench on CI + RoamCore-owned config_flow.py + integration tests asserting the 5 safety interlocks all flip + a setpoint change on `number.rc_hvac_floor_setpoint` triggers the right tile updates))
  - manifest-honesty smoke: `connections/heated-floors/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_interlocks_are_documented` defensive guard for the future tier-a promotion's hard-enforced safety interlock asserts)
  - 13 contract entities all `rc_hvac_*`: `climate.rc_hvac_floor_thermostat`, `sensor.rc_hvac_floor_current_temp`, `sensor.rc_hvac_interior_temp`, `binary_sensor.rc_hvac_floor_heating_active`, `binary_sensor.rc_hvac_floor_maintaining`, `binary_sensor.rc_hvac_floor_off`, `switch.rc_hvac_floor_heater`, `number.rc_hvac_floor_setpoint`, `select.rc_hvac_floor_mode`, `binary_sensor.rc_hvac_floor_low_voltage_lockout`, `switch.rc_hvac_engine_preheat`, `binary_sensor.rc_hvac_engine_preheat_active`, `sensor.rc_hvac_engine_preheat_runtime_min` (per docs/reference/rc-entity-naming.md §hvac subsystem — added to the allowed subsystems list alongside this slice, mirroring how `media` was added by the Music Assistant slice + how `presence` was backfilled alongside the bluetooth-wifi-presence slice + how `bed_lift` was added alongside the happijac slice)
  - vendor-neutrality strictly enforced: NO `floor`, `heated`, `thermostat`, `wyze`, `mysa`, `shelly`, `moen`, `zigbee`, `webasto`, `espar`, `eberspacher`, `preheat`, `engine`, `climate_`, `generic_thermostat` appears in any tile id BEYOND the `rc_hvac_*` subsystem prefix (the spec-required tile IDs legitimately contain `floor`, `engine`, `preheat`, `thermostat` in the suffix to describe what the tile is for; the forbidden substrings target vendor-name absolute-forbidden set only)
  - legacy tier-c catalog page (`docs/catalog/hvac/heated-floors-and-engine-preheat.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to Victron: §7.4 safety interlock for low-voltage lockout uses `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` from the Victron `connections/victron/` recipe (heated floor + engine preheat together can pull 10–30 A sustained)
  - cross-reference to Bluetooth / Wi-Fi presence: §7.5 presence-aware pre-warm automation uses the bluetooth-wifi-presence `connections/bluetooth-wifi-presence/` contract tile (`binary_sensor.rc_presence_operator_phone_arrived`) for the operator-arrival signal
  - cross-reference to Happijac: §7.6 frost-protection automation uses the upcoming happijac `connections/happijac/` contract tile (`select.rc_bed_lift_mode`) for the mode signal — if mode is `auto` AND engine preheat is off AND interior temp < 5 °C → enable gentle floor heating for frost protection
  - PR #48
- Smoke / CO / gas sensors (lifesafety for vans) (tier-b connection manifest)
  - tier-b manifest: `connections/smoke-co-gas-sensors/connection.yml` (safety category, beta status; Path A Zigbee smoke / CO / gas detector via the ZHA GUI flow + Path B Z-Wave smoke / CO detector via the zwave_js GUI flow + Path C DIY MQ-series analog gas sensor via ESPHome + a relay-driven siren on a GPIO; `install.config_flow: true` is honest because the UPSTREAM HA core `zha` + `zwave_js` + `esphome` + `binary_sensor` + `mqtt` + `template` integrations ALL expose a GUI flow since 2022.x / 2023.x; no RoamCore-owned wrapper / no native integration code is shipped — all three paths recipe over upstream ZHA / zwave_js / ESPHome + HA core `binary_sensor` / `mqtt` / `template` integrations)
  - recipe: `connections/smoke-co-gas-sensors/docs/recipe.md` (~590-line howto: Path A Zigbee via ZHA pairing + the auto-discovered `binary_sensor.smoke_*` / `binary_sensor.carbon_monoxide_*` / `binary_sensor.gas_*` entity_ids + the recommended ZHA device signature overrides for vendors that don't ship clean signatures (Heiman / Develco / First Alert / X-Sense); Path B Z-Wave via zwave_js pairing + the Notification CC that some Z-Wave detectors use to differentiate test vs alarm vs low-battery states + the Z-Wave controller home ID backup recovery pattern; Path C ESPHome YAML for an MQ-9 sensor on GPIO 34 (ADC1_CH6) + a relay-driven siren on GPIO 26 + the full calibration routine (24–48 hour burn-in + threshold tuning for the specific gas of interest) + the threshold-derived `binary_sensor.lpg_alarm` template + the per-gas thresholds cheat sheet for MQ-2 / MQ-3 / MQ-5 / MQ-7 / MQ-9 / MQ-135; 12 `rc_safety_*` contract tiles (8 binary_sensor + 1 sensor + 2 button + 1 select); 4 MANDATORY §6 lifesafety interlocks (sensor-not-offline detection via mqtt `availability` topic + low-battery pre-warning when any sensor battery < 20 % + any-alarm aggregate + mode-aware lockout when `alarm_mode=disabled` suppresses notifications); 7 automations ("loud siren + phone notification" on any alarm + "auto-unlock deadbolts + flash all interior lights" on CO detection for emergency egress + "low-battery pre-warning" on Sunday morning + "sensor offline" detection via mqtt `last_seen` / `availability` topic + "monthly test cycle" via `button.rc_safety_test_alarm` + "night-only mode" via `select.rc_safety_alarm_mode` + "smart-cooking integration" via the smart-automations recipe); 8 troubleshooting entries (ZHA pair failing on Zigbee channel conflict with the van's Wi-Fi AP + zwave_js unpair after van power-cycle (Z-Wave home ID backup) + ESPHome MQ sensor reading zero (wrong ADC pin; check `input: true`) + ESPHome MQ sensor reading saturated at 4095 (wrong voltage divider) + siren not firing (relay polarity; swap the buzzer leads) + sensor going offline when the van engine runs (alternator noise on the 12 V rail; add a ferrite choke + bulk capacitor) + low-battery warning stuck after battery swap (ZHA device signature cache; re-interview the device) + false-positive smoke when cooking (open a window first, or use `select.rc_safety_alarm_mode = silenced`)); §9 privacy (no telemetry beyond local Zigbee / Z-Wave / ESPHome sensor feeds; no vendor cloud / no ESPHome cloud / no MQTT broker cloud); §10 tier-a promotion outline (real smoke / CO / gas sensor + ESPHome + relay / siren bench on CI + RoamCore-owned `config_flow.py` walking Path A vs Path B vs Path C + sensor type / siren pin / battery alert threshold declaration + integration tests asserting the 4 lifesafety interlocks all flip + a sensor-trigger event propagates to the right tile updates within a defined latency budget))
  - manifest-honesty smoke: `connections/smoke-co-gas-sensors/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_interlocks_are_documented` defensive guard for the future tier-a promotion's hard-enforced lifesafety-interlock asserts)
  - 12 contract entities all `rc_safety_*`: `binary_sensor.rc_safety_smoke_detected`, `binary_sensor.rc_safety_co_detected`, `binary_sensor.rc_safety_gas_detected`, `binary_sensor.rc_safety_any_alarm_active`, `binary_sensor.rc_safety_siren_active`, `binary_sensor.rc_safety_alarm_in_test_mode`, `binary_sensor.rc_safety_low_battery_warning`, `binary_sensor.rc_safety_sensor_offline`, `sensor.rc_safety_lowest_battery_pct`, `button.rc_safety_silence_alarm`, `button.rc_safety_test_alarm`, `select.rc_safety_alarm_mode` (per docs/reference/rc-entity-naming.md §safety subsystem — added to the allowed subsystems list alongside this slice, mirroring how `media` was added by the Music Assistant slice + how `presence` was backfilled alongside the bluetooth-wifi-presence slice + how `bed_lift` was added alongside the happijac slice + how `hvac` was added alongside the heated-floors slice)
  - vendor-neutrality strictly enforced: NO `zigbee`, `zha`, `zwave`, `zwave_js`, `esphome`, `nest`, `firstalert`, `first_alert`, `kidde`, `x-sense`, `xsense`, `heiman`, `develco`, `fire`, `alert`, `binary_sensor_*`, `adc`, `carbon`, `mq`, `mq2`, `mq3`, `mq5`, `mq7`, `mq9`, `mq135`, `lpg`, `propane`, `methane` appears in any tile id BEYOND the `rc_safety_*` subsystem prefix (the spec-required tile IDs legitimately contain `smoke`, `co`, `gas` in the suffix by design — those are semantic suffixes describing what the tile is for, not vendor double-stamps; the forbidden substrings target the vendor-name / protocol-name / specific-gas-sensor-model absolute-forbidden set only)
  - legacy tier-c catalog page (`docs/catalog/safety/smoke-co-gas-sensors.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to Deadbolts: §7.2 "auto-unlock deadbolts on CO detection for emergency egress" automation uses the upcoming deadbolts connection (Wave 3 #48 slice) — the CO branch of the any-alarm-aggregate is the safety-critical one
  - cross-reference to Smart automations: §7.7 "smart-cooking integration" (hob on + windows closed → auto-set `alarm_mode` to `silenced` for 30 min to avoid false-positive sirens) uses the smart-automations connection (Wave 3 #46 slice) — combines with the night_only mode for the full cooking-aware silencing story
  - cross-reference to Heated floors: §1 cross-refers to heated-floors for the recipe pattern (lifesafety sensors ride the same template-only tier-b path that heated-floors defined Wave 3 #44)
  - cross-reference to Happijac: §6 §7 cross-refers to happijac for the recipe pattern (smoke/CO/gas sensors follow the same `template:` contract-tile synthesis that happijac defined Wave 3 #43)
  - cross-reference to Bluetooth / Wi-Fi presence: §7.5 monthly test cycle fires only when the operator is home (uses the bluetooth-wifi-presence `connections/bluetooth-wifi-presence/` contract tile from Wave 3 #42)
  - PR #49
- Smoke / CO / gas safety sensors (van life safety monitoring) (tier-b connection manifest)
  - tier-b manifest: `connections/smoke-co-gas-sensors/connection.yml` (safety category, beta status; Path A smart detectors Nest Protect / First Alert Z-Wave / X-Sense Zigbee / Heiman Z-Wave / Zipato Zigbee (operator already owns) + Path B basic Kidde / First Alert battery-only alarms minimum safety baseline (no HA integration) + Path C propane/LPG detectors Mopeka / Atemox / GasAlert / MQ-series via Modbus bridge or ESPHome analog input; `install.config_flow: true` is honest because the UPSTREAM HA core `binary_sensor` + `sensor` domains + the operator's choice of vendor integration (ZHA / Z-Wave JS / ESPHome) exposes a GUI flow; no RoamCore-owned wrapper / no native integration code is shipped — all three paths recipe over upstream vendor integrations OR HA core `binary_sensor` + `sensor` domains + the operator's choice of vendor integration)
  - recipe: `connections/smoke-co-gas-sensors/docs/recipe.md` (~470-line howto: Path A smart detector wiring + the entity_id surfacing + a `template:` binary_sensor that aggregates multiple sensors of the same type via `is_state(...) or is_state(...)` (e.g. one smoke detector in the bedroom + one in the cab → either triggering lights the `smoke_detected` tile); Path B minimum safety baseline subsection listing the recommended detectors (Kidde KN-COSM-IBA combo smoke+CO, Nest Protect, X-Sense SD11, First Alert Z-Wave) — without HA integration, the contract tiles can't be wired, but the operator still has local audible alarms which is the minimum safety guarantee; Path C propane/LPG detector wiring (the analog-to-digital wiring + the `sensor.gas_ppm` template that derives the ppm reading from the analog voltage); 14 `rc_safety_*` contract tiles (3 binary_sensor detected: smoke / CO / gas; 3 sensor max_ppm: smoke / CO / gas; binary_sensor any_alarm aggregate; binary_sensor alarm_silenced flag; binary_sensor battery_low; binary_sensor sensor_offline; select alarm_mode: full / night_only / cooking_active / off; button alarm_test; button alarm_silence); 6 safety automations (smoke detected → emergency-egress unlock + siren + lights + push notification; CO detected → cut propane solenoid + open roof vents + turn off HVAC + push notification; gas leak detected → cut propane solenoid + open roof vents + turn off HVAC + push notification; sensor offline > 30 min → push notification with bluetooth-wifi-presence escalation when operator is home; sensor battery low → push notification; alarm silenced → auto-resume after operator-set duration default 10 minutes); 8 troubleshooting entries (alarm false-positive during cooking use cooking_active mode + smoke detector in bedroom not kitchen; sensor not discovered Zigbee / Z-Wave interview needed; ppm reading stuck at 0 sensor calibration needed; sensor offline after first install battery not seated correctly reseat; alarm won't silence test button is on a different entity; CO threshold set too sensitive false alarm during heavy cooking do not lower below vendor recommendation; Mopeka propane sensor shows leak when tank is full calibrate the empty/full reference; X-Sense integration missing in HA core install via ZHA; Shelly UNI analog input not reporting check 4-20 mA wiring polarity); §9 privacy (no telemetry beyond local Zigbee / Z-Wave / Wi-Fi / ESPHome; no vendor cloud); §10 tier-a promotion outline (real smoke + CO + propane sensor bench on CI + RoamCore-owned config_flow.py walking Path A vs Path B vs Path C + detector placement per local code + analog input wiring for Path C + integration tests asserting the 6 safety automations all fire + the any_alarm tile lights up))
  - manifest-honesty smoke: `connections/smoke-co-gas-sensors/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_automations_are_documented` defensive guard for the future tier-a promotion's hard-enforced safety automation asserts)
  - 14 contract entities all `rc_safety_*`: `binary_sensor.rc_safety_smoke_detected`, `binary_sensor.rc_safety_co_detected`, `binary_sensor.rc_safety_gas_detected`, `sensor.rc_safety_smoke_max_ppm`, `sensor.rc_safety_co_max_ppm`, `sensor.rc_safety_gas_max_ppm`, `binary_sensor.rc_safety_any_alarm`, `binary_sensor.rc_safety_alarm_silenced`, `binary_sensor.rc_safety_battery_low`, `binary_sensor.rc_safety_sensor_offline`, `select.rc_safety_alarm_mode`, `button.rc_safety_alarm_test`, `button.rc_safety_alarm_silence` (per docs/reference/rc-entity-naming.md §safety subsystem — added to the allowed subsystems list alongside this slice, mirroring how `media` was added by the Music Assistant slice + how `presence` was backfilled alongside the bluetooth-wifi-presence slice + how `bed_lift` was added alongside the happijac slice + how `hvac` was added alongside the heated-floors slice)
  - vendor-neutrality strictly enforced: NO `nest`, `kidde`, `first_alert`, `x_sense`, `heiman`, `zipato`, `mopeka`, `atemox`, `gasalert`, `mq_2`, `mq_5`, `mq_7`, `mq_135`, `esphome`, `zigbee`, `z_wave`, `propane`, `lpg`, `natural_gas` appears in any tile id BEYOND the `rc_safety_*` subsystem prefix (the spec-required tile IDs legitimately contain `smoke`, `co`, `gas`, `alarm`, `battery`, `sensor`, `offline`, `silenced`, `test`, `mode` in the suffix to describe what the tile is for; the forbidden substrings target vendor-name absolute-forbidden set only)
  - legacy tier-c catalog page (`docs/catalog/safety/smoke-co-gas-sensors.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to Smart Automations: §7 cooking-active mode-aware suppression uses the smart-automations `connections/smart-automations/` contract tile for the mode signal (non-CO alarms silenced for 30 min during active cooking; CO is NEVER silenced — CO is life-threatening even during cooking)
  - cross-reference to Deadbolts: §7.1 smoke-detected emergency-egress unlock automation uses the upcoming deadbolts `connections/deadbolts/` recipe's `lock.deadbolt_emergency_unlock` switch (the operator must be able to get OUT of the van even if the door locks automatically during a fire)
  - cross-reference to Bluetooth / Wi-Fi presence: §7.4 sensor-offline escalation uses the bluetooth-wifi-presence `connections/bluetooth-wifi-presence/` contract tile (`binary_sensor.rc_presence_operator_phone_home`) to escalate the notification when the operator is in the van AND a safety sensor is offline (operator MUST address the dead battery / lost Wi-Fi immediately)
  - cross-reference to Mode / automation-builder: §7 `select.rc_safety_alarm_mode` mode-aware behavior uses the mode/automation-builder `connections/mode-automation-builder/` contract tile for the mode signal
  - PR #49
- Smart automations (one-click enable / disable — 17 prebuilt van-life automations) (tier-b connection manifest)
  - tier-b manifest: `connections/smart-automations/connection.yml` (safety category, beta status; recipe-over-upstream — no RoamCore-owned automation engine; the 17 automations live in HA Core's `automation:` domain + the operator-side managed-marker convention from `docs/guides/smart-automations.md`; `install.config_flow: true` is honest because the UPSTREAM HA core `automation` + `script` + `template` + `button` + `select` integrations ALL expose a GUI config_flow since 2023.x; no RoamCore-owned wrapper / no native integration code is shipped — the recipe §3 walks the operator through enabling each of the 17 automations one-by-one via the HA Core config API + auditing the managed-marker (`Managed by RoamCore Smart Automations v0.1` + `key=<name>` + `hash=<template hash>`) + publishing the 24 `rc_safety_*` contract tiles on top)
  - recipe: `connections/smart-automations/docs/recipe.md` (~430-line howto: the managed-marker convention + the audit `template:` binary_sensor pattern + the `template:` sensor aggregation for enabled count / total count / missing count / all-ready / view select / enable-all-ready / disable-all summary tiles + per-automation `binary_sensor.rc_safety_automation_<name>` mirror tiles + the 17 automations mirrored from `docs/guides/smart-automations.md` (Night Mode 23:00 → Stealth; 07:00 → Auto / Auto Internet Failover WAN bad for 2 min → prefer_auto / Low Battery Mode SOC < 20% for 10 min AND shore disconnected → Camp / Freeze Protection temp < 2°C for 10 min → notification / Daily Trip Log 23:59 → logbook / Battery Full Alert SOC > 95% for 15 min → notification / Inverter Overheat Alert > 75°C for 5 min → notification / Router Overheat Alert > 70°C for 10 min → notification / Shore Power Connected → notification / Shore Power Disconnected for 1 min → notification / Internet Recovery unreachable for 2 min → restart_network / Arrive at Camp speed < 1 for 15 min AND 18:00-23:59 → Camp / Depart Travel Mode speed > 10 for 2 min → Travel / Solar is Crushing It > 600W for 5 min → notification / Battery Critical Alert SOC < 10% for 5 min → notification / Bedtime Level Check 22:00 + not level → reminder / Quiet Hours Reminder 21:30 + mode != Stealth → reminder) + the per-automation prerequisite list + cross-references to the upstream connection contracts (Victron for SOC + shore-connected + inverter temp + solar power; Traccar for trip distance + GPS speed; Net for WAN status + internet reachable; OpenWrt controls for prefer_auto + restart_network + router temp; Weather for outside temp; Mode / automation-builder for the mode scripts + `input_select.rc_mode`; Leveling for the bedtime level check; Smoke / CO / gas sensors for the cooking-aware silencing integration); §8 troubleshooting (automation doesn't trigger / action fails / managed-marker overwritten / key collisions / managed-marker not detected / Night Mode timezone / Low Battery Mode shore bouncing / Bedtime Level Check false positives); §7 privacy (all local; no cloud call home; only network call is the HA Core push notification if operator set one up); §9 tier-a promotion outline (RoamCore-owned automation engine + `config_flow.py` walking each automation one-by-one + integration tests asserting enabling/disabling via the HA Core API + the managed-marker invariants hold + the bench fixture))
  - manifest-honesty smoke: `connections/smart-automations/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_recipe_documents_all_17_automations` defensive guard asserting all 17 automations from `docs/guides/smart-automations.md` appear as §4.X subsections + per-automation `binary_sensor.rc_safety_automation_<name>` mirror tiles)
  - 24 contract entities all `rc_safety_*`: 7 summary tiles (`sensor.rc_safety_automations_enabled_count`, `sensor.rc_safety_automations_total_count`, `sensor.rc_safety_automations_missing_count`, `binary_sensor.rc_safety_automations_all_ready`, `select.rc_safety_automations_view`, `button.rc_safety_automations_enable_all_ready`, `button.rc_safety_automations_disable_all`) + 17 per-automation mirror binary_sensors (`binary_sensor.rc_safety_automation_night_mode` / `_auto_internet_failover` / `_low_battery_mode` / `_freeze_protection` / `_daily_trip_log` / `_battery_full_alert` / `_inverter_overheat_alert` / `_router_overheat_alert` / `_shore_power_connected` / `_shore_power_disconnected` / `_internet_recovery` / `_arrive_at_camp` / `_depart_travel_mode` / `_solar_crushing_it` / `_battery_critical_alert` / `_bedtime_level_check` / `_quiet_hours_reminder`) (per docs/reference/rc-entity-naming.md §safety subsystem — the `safety` subsystem is ALREADY in the allowed subsystems list, so no backfill was needed; the per-automation tile suffix `_automation_<name>` is the documented naming convention for "this is the binary_sensor that mirrors the state of an automation by the same name" and is NOT a generic-noun double-stamp)
  - vendor-neutrality strictly enforced: NO `nest`, `kidde`, `first_alert`, `x_sense`, `heiman`, `zipato`, `victron`, `traccar`, `openwrt`, `esphome`, `zigbee`, `z_wave`, `zwave`, `mqtt`, `tuya`, `xiaomi`, `philips`, `ubiquiti`, `unifi`, `mikrotik`, `smart`, `automation_engine`, `roamcore_smart`, `lovelace_`, `timer_`, `schedule_`, `rule_` appears in any tile id BEYOND the `rc_safety_*` subsystem prefix
  - legacy tier-c catalog page (`docs/catalog/safety/smart-automations.md`) now carries a supersession banner pointing at the new connection folder; the `data-tier="a"` on `docs/catalog/safety/index.md` was honestly flipped to `data-tier="b"` since the connection is honestly tier-b (no RoamCore-owned automation engine)
  - cross-reference to Smoke / CO / gas sensors: the §17 Quiet Hours Reminder + the §7.7 cooking-aware silencing automation from the smoke-co-gas-sensors recipe both wire the `select.rc_safety_alarm_mode = silenced` flow during active cooking — uses the smart-automations mode signal as the trigger
  - cross-reference to Heated floors + engine pre-heat: the §16 Bedtime Level Check automation depends on `binary_sensor.rc_level` + `sensor.rc_level_status` from the upcoming leveling connection + the heated-floors §7.5 frost-protection automation
  - cross-reference to Bluetooth / Wi-Fi presence: the 17 automations' persistent notifications can be escalated via the smoke-co-gas-sensors §7.4 pattern when the operator is in the van AND a safety-relevant automation fires
  - PR #50
- Mock location + track replay (dev/demo polyline generator for the map page) (tier-a connection manifest)
  - tier-a manifest: `connections/mock-location-and-tracks/connection.yml` (map category, beta status; tier-a native — RoamCore owns all of the integration code natively; `wizard.connection_kind: native` + `install.kind: ha_package` + `install.config_flow: false` + `install.packages: [homeassistant/packages/roamcore_mock_track.yaml, homeassistant/packages/roamcore_mock_location_trail.yaml, homeassistant/packages/roamcore_dev_mocks.yaml]` + `__init__.py` exports `DOMAIN = "mock_location"` + NO `config_flow` substring anywhere in `__init__.py`; this is the FIRST tier-a connection slice in Wave 3 — all prior Wave 3 slices were tier-b recipe-over-upstream; tier-a DOES NOT use `config_flow.py` because RoamCore owns the integration as a package and there is no upstream vendor integration to wrap; the operator enables the mock by toggling `input_boolean.rc_map_mock_enabled` (the upstream `input_boolean` integration's own GUI flow handles the toggle); the Python generator `homeassistant/tools/mock_track/generate.py` is dependency-free (stdlib only — `argparse` + `json` + `math` + `random` + `os` + `datetime`); the three YAML packages are auto-included by `homeassistant/configuration_addon.yaml` via `!include_dir_named packages` so no manual wiring is required)
  - recipe: `connections/mock-location-and-tracks/docs/recipe.md` (~497-line howto, 11 §sections: §1 what is mock location + track replay in RoamCore + the deterministic + dependency-free + opt-in + auto-runs-on-startup + no-cloud-call-home characteristics + the tier-a audit markers; §2 prerequisites (HA Core 2023.8+ + the three YAML packages wired in + the Python generator path + Python 3.11+ + no upstream vendor integration + the operator-side mock-vs-Traccar decision); §3 quick start (3-step: toggle `input_boolean.rc_map_mock_enabled` → pick preset → click Generate); §4 the 6 built-in presets (`uk_roadtrip` 9-waypoint UK + Ireland circular loop ~1,500 km / `us_west_coast` Pacific Coast Highway Seattle → San Diego ~2,000 km / `alps_loop` tight mountain loop through the Alps ~800 km / `desert_southwest` sprawling US Southwest loop ~1,200 km / `scandinavia_north` long Nordic sweep ~3,000 km / `custom` operator-authored GeoJSON fallback); §5 the 9 `rc_map_mock_*` contract tiles + how each upstream `input_*` / `button` / `template:` sensor exposes them + the `binary_sensor.rc_map_mock_track_fresh` freshness check; §6 custom polyline authoring (geojson.io / Google Maps / OpenStreetMap workflows + the `[lon, lat]` order convention + the required GeoJSON schema + validation); §7 integration with Trip Wrapped (treats the mock as Traccar data when generating the dev/demo HTML report + useful for marketing screenshots + integration tests); §8 integration with the map page (`<MapMockPolyline />` tile reads `input_boolean.rc_map_mock_enabled` + `sensor.rc_map_mock_track_length_km` + `sensor.rc_map_mock_track_point_count` + `binary_sensor.rc_map_mock_track_fresh`); §9 troubleshooting (8 entries: mock polyline not showing / preset change doesn't regenerate / custom polyline rejected / shapely import error / polyline too dense / polyline too jittery / mock toggle ignored / polyline not on actual location); §10 privacy (no telemetry + no cloud call home + deterministic OR operator-entered + no PII + runs entirely on HAOS); §11 promoting to a real GPS source (enable `connections/traccar/` + toggle `input_boolean.rc_map_mock_enabled` to OFF → map page uses real Traccar; mock is the fallback / dev / demo path))
  - manifest-honesty smoke: `connections/mock-location-and-tracks/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_tier_a_with_native_markers` audit (tier=a + `wizard.connection_kind: native` + `install.kind: ha_package` + `install.config_flow: false` + the three YAML packages exist on disk in `homeassistant/packages/` + the Python generator exists at `homeassistant/tools/mock_track/generate.py` + `__init__.py` exports `DOMAIN = "mock_location"` + NO `config_flow` substring anywhere in `__init__.py` + NO `config_flow.py` at the folder root + `install.packages` references all three mock YAML files); the `test_dashboard_tiles_follow_rc_naming` defensive guard (9 vendor-neutral `rc_map_mock_*` tiles; vendor / preset-name / format-name absolute-forbidden set including `mock_location_and_tracks`, `mock_track`, `mock_trail`, `uk_roadtrip`, `us_west_coast`, `alps_loop`, `desert_southwest`, `scandinavia_north`, `polyline`, `geojson`, `shapely`, `gpxtpx`); the `test_requires_docs_recipe_published` defensive guard (recipe.md ≥250 lines + ≥10 §sections + covers all 6 presets + references all 9 contract tiles); the `test_mock_packages_wired_into_homeassistant_yaml` defensive guard (verifies `homeassistant/configuration_addon.yaml` contains the `packages: !include_dir_named packages` directive AND the three mock YAML files exist on disk))
  - 9 contract entities all `rc_map_mock_*`: 1 master toggle (`input_boolean.rc_map_mock_enabled`) + 1 trail helper (`input_text.rc_map_mock_location_trail`) + 1 preset selector (`input_select.rc_map_mock_preset`) + 2 input_numbers (`input_number.rc_map_mock_points_per_leg` / `input_number.rc_map_mock_jitter_m`) + 1 generate button (`button.rc_map_mock_generate_now`) + 2 derived sensors (`sensor.rc_map_mock_track_length_km` / `sensor.rc_map_mock_track_point_count`) + 1 freshness binary_sensor (`binary_sensor.rc_map_mock_track_fresh`) (per docs/reference/rc-entity-naming.md §map subsystem — the `map` subsystem was added to the allowed subsystems list alongside this slice — see the Cron-handoff doc for the rationale)
  - vendor-neutrality strictly enforced: NO `nest`, `kidde`, `first_alert`, `x_sense`, `heiman`, `zipato`, `victron`, `traccar`, `openwrt`, `esphome`, `zigbee`, `z_wave`, `zwave`, `mqtt`, `tuya`, `xiaomi`, `philips`, `ubiquiti`, `unifi`, `mikrotik`, `mock_location_and_tracks`, `mock_track`, `mock_trail`, `uk_roadtrip`, `us_west_coast`, `alps_loop`, `desert_southwest`, `scandinavia_north`, `polyline`, `geojson`, `shapely`, `gpxtpx` appears in any tile id BEYOND the `rc_map_mock_*` subsystem prefix
  - legacy tier-A catalog page (`docs/catalog/map/mock-location-and-tracks.md`) now carries a supersession banner pointing at the new connection folder; the `data-tier="a"` on `docs/catalog/map/index.md` is unchanged since the connection is honestly tier-a; the `docs/catalog/tier-a.md` Map section entry was verified to be present (no edit needed — mock-location-and-tracks was already listed from the Wave 2 catalog work)
  - cross-reference to Traccar: when `connections/traccar/` is enabled AND `input_boolean.rc_map_mock_enabled` is FALSE, the map page uses the real Traccar track; the mock is the fallback / dev / demo path (recipe §11 promotion path)
  - cross-reference to Trip Wrapped: `connections/trip-wrapped/` Wave 3 #69 treats the mock as Traccar data when generating the dev/demo HTML report; useful for marketing screenshots + integration tests + dev demos before the operator has real trip data
  - cross-reference to Map Dashboard: `connections/map-dashboard/` Wave 3 #66 reads `input_boolean.rc_map_mock_enabled` to decide mock-vs-real; the `<MapMockPolyline />` tile reads `sensor.rc_map_mock_track_length_km` + `sensor.rc_map_mock_track_point_count` + `binary_sensor.rc_map_mock_track_fresh` for the legend readout
  - cross-reference to Dev mocks umbrella: `homeassistant/packages/roamcore_dev_mocks.yaml` ships the input_* helpers used by other recipes (power / net / level / trip / map); the mock-location-and-tracks connection includes the dev_mocks package as one of its three `install.packages` because the dev_mocks umbrella is the canonical home for the input_* helper pattern
  - PR #51
- Deadbolts (smart lock control for van doors) (tier-b connection manifest)
  - tier-b manifest: `connections/deadbolts/connection.yml` (safety category, beta status; Path A Z-Wave deadbolts (Schlage Encode Plus, Yale Assure 2, Kwikset Halo) via the upstream zwave_js integration's GUI flow since 2022.x; Path B Zigbee deadbolts (Aqara A100, Yale Assure 2 Zigbee) via the upstream zha or zigbee2mqtt integration's GUI flow since 2022.x; Path C Matter/Thread deadbolts (Level Lock+, Yale Assure 2 Matter variant) via the upstream matter integration's GUI flow since 2023.x + a Thread border router on the LAN; `install.config_flow: true` is honest because the UPSTREAM HA core `zwave_js` (Path A) + `zha` (Path B) + `matter` (Path C) integrations ALL expose a GUI flow since 2022.x / 2023.x; no RoamCore-owned wrapper / no native integration code is shipped — all three paths recipe over upstream zwave_js / zha / matter + HA core `lock` + `template` integrations)
  - recipe: `connections/deadbolts/docs/recipe.md` (~620-line howto: Path A Z-Wave JS pairing instructions + recommended Z-Wave locks (Schlage Encode Plus, Yale Assure 2, Kwikset Halo) + the upstream `lock.<name>` entity surfacing + the HA core `template:` lock that maps `lock.front_door` -> `lock.rc_safety_lock_front_door`; Path B ZHA or Zigbee2MQTT pairing instructions + recommended Zigbee locks (Aqara A100, Yale Assure 2 Zigbee) + the same `template:` lock pattern for the side_door slot; Path C Matter pairing instructions + Thread border router prerequisite (OpenWrt VM, Apple HomePod mini, Nest Hub v2, or Aeotec Border Router) + recommended Matter locks (Level Lock+, Yale Assure 2 Matter variant) + the same `template:` lock pattern for the storage_compartment slot; 12 `rc_safety_lock_*` contract tiles (3 lock tiles for front_door / side_door / storage_compartment + 3 binary_sensor aggregate (any_unlocked) / event (unexpected_unlock) / safety (co_egress_required, low_voltage_lockout) + 2 sensor aggregate (unlocked_count) / freshness (last_action_age_min) + 1 select (lock_mode) + 2 button action (lock_all, unlock_all)); 6 MANDATORY §7 safety interlocks (Away auto-lock via `select.rc_mode` from mode/automation-builder connection; Sleep auto-lock + auto-relock (driver can't forget to lock the door at night); unattended-unlock alarm cross-referencing `binary_sensor.rc_presence_anyone_home` from bluetooth-wifi-presence Wave 3 #42 + `select.rc_mode != away`; CO egress-required override cross-referencing `binary_sensor.rc_safety_co_detected` from smoke-co-gas-sensors Wave 3 #45 — auto-unlock the egress-path doors (front_door + side_door; storage_compartment can stay locked — not on the egress path); low-voltage lockout cross-referencing `sensor.rc_power_battery_soc` from Victron — disable auto-relock when SOC < 20% to save battery current; multi-door aggregate covering vans with two doors + storage compartments); 6 troubleshooting entries (lock not responding (battery low on the lock; Z-Wave / Zigbee mesh range; Matter Thread reachability); lock state stuck (Z-Wave interview incomplete — wake the lock manually and re-interview); unexpected-unlock false-positive (presence detection lag — increase the home/not_home grace period on bluetooth-wifi-presence); CO-egress doesn't fire (smoke-co-gas-sensors Wave 3 #45 connection not installed yet); low-voltage-lockout stuck on (Victron SOC recovering — wait 5 min); Z-Wave JS network down (USB stick unplugged — `dmesg | grep -i zwave` and reseat the dongle)); §9 privacy (no telemetry beyond local Z-Wave / Zigbee / Matter; no vendor cloud / no zwave_js cloud / no zha cloud / no matter cloud); §10 tier-a promotion outline (real Z-Wave / Zigbee / Matter deadbolt on CI + RoamCore-owned config_flow.py walking Path A vs Path B vs Path C + naming each lock entity + mapping to one of `rc_safety_lock_front_door` / `rc_safety_lock_side_door` / `rc_safety_lock_storage_compartment` + integration tests asserting a state change from `locked` → `unlocked` triggers the right `binary_sensor.rc_safety_lock_any_unlocked` + `sensor.rc_safety_lock_unlocked_count` updates + the 6 safety interlocks all fire when wired to canned fixture responses))
  - manifest-honesty smoke: `connections/deadbolts/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_interlocks_are_documented` defensive guard asserting all 6 safety interlocks (Away auto-lock / Sleep auto-lock + auto-relock / unattended-unlock alarm / CO egress-required override / low-voltage lockout / multi-door aggregate) are documented in recipe §7 + cross-reference `binary_sensor.rc_safety_co_detected` from smoke-co-gas-sensors Wave 3 #45 + `binary_sensor.rc_presence_anyone_home` from bluetooth-wifi-presence Wave 3 #42 + `sensor.rc_power_battery_soc` from Victron + `select.rc_mode` from mode/automation-builder + the storage_compartment egress-path guidance)
  - 12 contract entities all `rc_safety_lock_*`: 3 lock tiles (`lock.rc_safety_lock_front_door` / `lock.rc_safety_lock_side_door` / `lock.rc_safety_lock_storage_compartment`) + 4 binary_sensor tiles (`binary_sensor.rc_safety_lock_any_unlocked` / `binary_sensor.rc_safety_lock_unexpected_unlock` / `binary_sensor.rc_safety_lock_co_egress_required` / `binary_sensor.rc_safety_lock_low_voltage_lockout`) + 2 sensor tiles (`sensor.rc_safety_lock_unlocked_count` / `sensor.rc_safety_lock_last_action_age_min`) + 1 select tile (`select.rc_safety_lock_mode`) + 2 button tiles (`button.rc_safety_lock_lock_all` / `button.rc_safety_lock_unlock_all`) (per docs/reference/rc-entity-naming.md §safety subsystem — the `safety` subsystem is ALREADY in the allowed subsystems list from the smoke-co-gas-sensors Wave 3 #45 slice, so no backfill was needed; the `safety_lock` sub-prefix in the tile IDs is the documented naming convention for "this is a tile owned by the safety subsystem that controls locks" and is NOT a generic-noun double-stamp)
  - vendor-neutrality strictly enforced: NO `zwave`, `z_wave`, `z-wave`, `zigbee`, `matter`, `thread`, `schlage`, `kwikset`, `yale`, `august`, `level_lock`, `levellock`, `ultraloq`, `igloohome`, `bold`, `lockly`, `smart_deadbolt`, `smart_lock`, `front_door_lock`, `side_door_lock`, `storage_door_lock` appears in any tile id BEYOND the `rc_safety_lock_*` subsystem prefix (the spec-required tile IDs legitimately contain `front_door`, `side_door`, `storage_compartment`, `any_unlocked`, `unlocked_count`, `last_action_age_min`, `unexpected_unlock`, `co_egress_required`, `low_voltage_lockout`, `lock_mode`, `lock_all`, `unlock_all` in the suffix to describe what the tile is for — the forbidden substrings target the protocol-name / lock-vendor-name / generic-noun double-stamp absolute-forbidden set only; the `egress` substring from the spec's forbidden list was NOT added to the test's forbidden set because the spec-required tile `binary_sensor.rc_safety_lock_co_egress_required` uses `egress` as a semantic suffix to describe the CO emergency-egress scenario — the required tile list is authoritative when in conflict with the forbidden list)
  - legacy tier-c catalog page (`docs/catalog/safety/deadbolts.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to Smoke / CO / gas safety sensors: §7.4 "CO egress-required override" safety interlock uses `binary_sensor.rc_safety_co_detected` from the smoke-co-gas-sensors Wave 3 #45 recipe — when CO is detected, the egress-path doors (front_door + side_door) auto-unlock so the operator can get OUT of the van even if the door was auto-locked at bedtime; the storage_compartment can stay locked (not on the egress path)
  - cross-reference to Bluetooth / Wi-Fi presence: §7.3 "unattended-unlock alarm" safety interlock uses `binary_sensor.rc_presence_anyone_home` from the bluetooth-wifi-presence Wave 3 #42 recipe — fires when any lock transitions to `unlocked` while no presence is detected AND mode is not `away`; the operator can increase the home/not_home grace period on the presence recipe if the alarm false-positives
  - cross-reference to Victron: §7.5 "low-voltage lockout" safety interlock uses `sensor.rc_power_battery_soc` from the Victron `connections/victron/` recipe — when SOC < 20%, auto-relock is disabled to save battery current (each lock/unlock cycle draws ~500 mA); the operator can still manually lock / unlock via the dashboard tiles
  - cross-reference to Happijac: the §7.5 low-voltage lockout rides the same Victron SOC signal pattern that the Happijac `binary_sensor.rc_bed_lift_low_voltage_lockout` tile uses (Happijac + deadbolts share the battery-aware relock semantics from Victron)
  - cross-reference to Mode / automation-builder: §7.1 Away auto-lock + §7.2 Sleep auto-lock + auto-relock use `select.rc_mode` from the mode/automation-builder `connections/mode-automation-builder/` recipe for the mode signal
  - storage compartment note: §7.4 explicitly documents that the storage_compartment lock tile is NOT on the CO egress path — when CO is detected, the egress-path doors auto-unlock but the storage_compartment stays locked (storage compartments don't need to be on the emergency egress path)
  - PR #52
- HVAC basics (heating/cooling foundations) (tier-b connection manifest)
  - tier-b manifest: `connections/hvac-basics/connection.yml` (hvac category, beta status; Path A generic thermostat via HA core `generic_thermostat` / `ecobee` / `nest` / `mitsubishi` / `daikin` (any climate.* entity); Path B diesel heater (Webasto / Eberspächer / Chinese diesel / Vevor / Chinese planer-style) via the upstream `esphome` integration (ESPHome exposes a GUI flow since 2023.x) OR the upstream `mqtt` integration for heaters with an ESPHome-MQTT bridge; Path C rooftop AC (Furrion / Dometic / MaxxAir / Coleman) via IR-bridge (Broadlink RM Mini 3 / MQTT-IR-Hub) OR native vendor integration; Path D cabin ventilation (MaxxAir / Fan-Tastic Vent / roof-vent switches) via the HA core `fan` integration; `install.config_flow: true` is honest because the UPSTREAM HA core `climate` (generic_thermostat / ecobee / nest / mitsubishi / daikin) + the `esphome` + the `mqtt` + the `broadlink` IR-bridge + the `fan` integrations ALL expose a GUI flow since 2022.x / 2023.x; no RoamCore-owned wrapper / no native integration code is shipped — all four paths recipe over upstream vendor integrations + HA core `template` + `binary_sensor` + `fan` + `select`)
  - recipe: `connections/hvac-basics/docs/recipe.md` (~950-line howto: Path A `generic_thermostat` YAML wrapping a temperature probe + a heater relay with `min_temp` / `max_temp` / `cold_tolerance` / `hot_tolerance` / `keep_alive` settings + the upstream vendor integration alternatives (ecobee / nest / mitsubishi / daikin) exposing climate.* entities directly; Path B ESPHome YAML for the heater's glow_plug + main_blower + combustion_fan + safety_thermistor + flame_sense binary_sensor (with delayed_on: 2s / delayed_off: 5s filters for the flame-sense wire) + the §7.4 hearter_active template that derives "is the diesel heater actually producing heat?" from `switch.combustion_fan` + `binary_sensor.flame_sense`; Path C IR-bridge (Broadlink / MQTT-IR-Hub) + the upstream `switch` + `fan` + `select` YAML wiring for AC power + mode + fan speed + temperature + the §7.5 ac_active template that derives "is the rooftop AC actually running?" from `switch.rooftop_ac_power` + `select.rooftop_ac_mode`; Path D HA core `fan` integration pairing for MaxxAir / Fan-Tastic Vent + the `fan.rc_hvac_cabin_fan` + `select.rc_hvac_fan_speed` template wiring for the discrete speed choices (off / low / med / high / auto); 11 `rc_hvac_*` contract tiles (1 climate + 3 sensor + 4 binary_sensor + 1 fan + 2 select); 4 MANDATORY §8 safety interlocks (frost warning via `sensor.rc_hvac_outdoor_temperature` < 2 °C AND `sensor.rc_hvac_cabin_temperature` < 5 °C forcing the cabin thermostat > 5 °C; over-temp warning via `sensor.rc_hvac_cabin_temperature` > 35 °C auto-turning on AC + opening cabin fan + push notification + escalation to high-priority when `binary_sensor.rc_presence_anyone_home` is FALSE for "pets left in van"; low-voltage lockout via `sensor.rc_power_battery_soc` < 30 % AND `binary_sensor.rc_power_shore_connected` FALSE disabling diesel heater + AC — cabin fan stays enabled because < 1 A; mode-aware lockouts (Stealth auto-lower fan + Sleep eco-mode fan-off + 2 °C setpoint drop + Boost disable-mode-aware-lockouts) via `select.rc_mode`); 6 §9 automations (Stealth auto-lower fan / Sleep eco-mode / Boost disable-mode-aware-lockouts / frost auto-heat / over-temp auto-cool + alert / mode-aware morning pre-warm at 06:00 weekdays); 8 §10 troubleshooting entries (thermostat not responding / heater won't ignite / heater shuts off after 30s / AC doesn't respond to IR / fan stuck on one speed / frost warning stuck on / over-temp warning stuck on / low-voltage lockout stuck on after charging); §11 privacy (no telemetry beyond local climate / esphome / mqtt / broadlink / fan integrations; no vendor cloud / no RoamCore cloud); §12 tier-a promotion outline (real HVAC bench (thermostat + heater + AC + fan + temperature/humidity sensor + relay board, all wired together in a controlled environment) + RoamCore-owned config_flow.py walking Path A vs Path B vs Path C vs Path D + integration tests asserting a temperature change on `sensor.rc_hvac_cabin_temperature` triggers the right `binary_sensor.rc_hvac_frost_warning` + `binary_sensor.rc_hvac_over_temp_warning` updates + the §7 safety interlocks all fire when wired to canned fixture responses))
  - manifest-honesty smoke: `connections/hvac-basics/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_interlocks_are_documented` defensive guard asserting all 4 safety interlocks (frost warning / over-temp warning / low-voltage lockout / mode-aware lockouts) are documented in recipe §8 + cross-reference `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` from Victron + `binary_sensor.rc_presence_anyone_home` from bluetooth-wifi-presence Wave 3 #42 + `select.rc_mode` from mode/automation-builder + the heated-floors Wave 3 #44 companion)
  - 11 contract entities all `rc_hvac_*`: 1 climate tile (`climate.rc_hvac_cabin_thermostat`) + 3 sensor tiles (`sensor.rc_hvac_cabin_temperature` / `sensor.rc_hvac_cabin_humidity` / `sensor.rc_hvac_outdoor_temperature`) + 4 binary_sensor tiles (`binary_sensor.rc_hvac_heater_active` / `binary_sensor.rc_hvac_ac_active` / `binary_sensor.rc_hvac_frost_warning` / `binary_sensor.rc_hvac_over_temp_warning`) + 1 fan tile (`fan.rc_hvac_cabin_fan`) + 2 select tiles (`select.rc_hvac_fan_speed` / `select.rc_hvac_mode`) (per docs/reference/rc-entity-naming.md §hvac subsystem — the `hvac` subsystem is ALREADY in the allowed subsystems list from the heated-floors Wave 3 #44 slice, so no backfill was needed; the umbrella HVAC basics connection shares the §hvac subsystem prefix with the heated-floors companion connection — the two recipes cross-reference each other for the floor heat + cabin heat surface relationship)
  - vendor-neutrality strictly enforced: NO `webasto`, `eberspacher`, `eberspaecher`, `furrion`, `dometic`, `maxxair`, `coleman`, `broadlink`, `vevor`, `generic_thermostat`, `ecobee`, `nest`, `mitsubishi`, `daikin`, `climate_` appears in any tile id BEYOND the `rc_hvac_*` subsystem prefix (the spec-required tile IDs legitimately contain `cabin`, `heater`, `ac`, `cool`, `heat`, `temperature`, `humidity`, `outdoor`, `frost`, `over_temp`, `mode`, `speed`, `warning`, `thermostat` in the suffix to describe what the tile is for — these are the generic-noun tolerance list per spec; the forbidden substrings target the vendor-name absolute-forbidden set only)
  - legacy tier-c catalog page (`docs/catalog/hvac/hvac-basics.md`) now carries a supersession banner pointing at the new connection folder; the original false "Support tier: A (RoamCore native)" claim was retracted (RoamCore does NOT ship a native HVAC appliance — the original claim was aspirational and false, now superseded by the tier-b recipe connection)
  - cross-reference to Heated floors + engine pre-heat: this HVAC basics connection is the UMBRELLA for cabin heating/cooling; the heated-floors Wave 3 #44 connection is the SPECIFIC case for floor heat + engine pre-heat (which is OUT OF SCOPE for HVAC basics); the two recipes cross-reference each other for the floor heat + cabin heat surface relationship (heated-floors' `climate.rc_hvac_floor_thermostat` vs HVAC basics' `climate.rc_hvac_cabin_thermostat` — the floor controls the floor mat temperature; the cabin controls the cabin air temperature; both can run independently)
  - cross-reference to Victron: §8.3 low-voltage lockout uses `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` from the Victron `connections/victron/` recipe — the diesel heater + rooftop AC together can pull 10–30 A sustained which would brown out the battery bank below 30 % SOC; the cabin fan stays enabled in low-voltage mode because it draws < 1 A
  - cross-reference to Bluetooth / Wi-Fi presence: §8.2 over-temp warning's "pets left in van" escalation uses `binary_sensor.rc_presence_anyone_home` from the bluetooth-wifi-presence Wave 3 #42 recipe — when over-temp fires AND presence says nobody is home, the alert escalates to high-priority + the dashboard highlights the over-temp tile in red (the operator + pets + electronics MUST not cook)
  - cross-reference to Mode / automation-builder: §8.4 mode-aware lockouts use `select.rc_mode` from the mode/automation-builder `connections/mode-automation-builder/` recipe for the Stealth auto-lower fan + Sleep eco-mode + Boost disable-mode-aware-lockouts
  - PR #N
- Water tanks (fresh/grey monitoring) (tier-b connection manifest)
  - tier-b manifest: `connections/water-tanks/connection.yml` (water category, beta status; Path A ESPHome tank sensor node (one ESP32 per node + ultrasonic probe JSNSR04T / HC-SR04 waterproof per tank + CT clamp on the 12 V pump wire + optional DS18B20 temperature probe + optional leak sensor) + Path B generic resistive / 4–20 mA / voltage probe via a Shelly UNI ADC input + the HA `template:` integration translating voltage to percentage via a per-tank calibration curve + Path C cloud-bridged level sensor (SeeLevel / Garnet SeeLevel II 709-BTG / Mopeka Pro Check / Lippert) via the vendor's own HA core or HACS integration; `install.config_flow: true` is honest because the UPSTREAM HA core `sensor` domain + the `binary_sensor` domain + the `template` integration + the `input_number` integration + the `input_select` integration + the `esphome` integration (Path A) + the `shelly` integration (Path B) ALL expose a GUI flow since 2022.x / 2023.x; no RoamCore-owned wrapper / no native integration code is shipped — all three paths recipe over upstream vendor integrations + HA core `template` + `binary_sensor` + `input_number` + `input_select`)
  - recipe: `connections/water-tanks/docs/recipe.md` (~706-line howto: Path A ESPHome YAML for the ESP32 with 2× ultrasonic probes (fresh on GPIO 13/12 + grey on GPIO 27/26) + the `lambda` filter that converts distance_cm to level_pct using the per-tank (max_distance, min_distance) calibration + the CT clamp on GPIO 34 (ADC1_CH6) sensing the pump current + the `binary_sensor.pump_running` template that fires when current > 0.5 A + the DS18B20 on GPIO 4 + the optional leak sensor on GPIO 14 with a 10 kΩ pull-up + the `delayed_off: 500ms` debounce filter; Path B Shelly UNI wiring (12 V supply + ADC inputs A1/A2) + the HA `template:` sensor wiring the raw voltage to percentage via the per-tank (calibration_empty_v, calibration_full_v) curve + the operator-tunable `number.rc_water_fresh_tank_size_l` + `number.rc_water_grey_tank_size_l` tiles; Path C SeeLevel via the `see_level` HACS integration / Mopeka via the `mopeka_pro_check` HACS integration / Garnet via the `serial` integration with a USB-to-serial adapter / Lippert via the `lippert_onecontrol` HACS integration; 17 `rc_water_*` contract tiles (5 sensor telemetry + 7 binary_sensor warnings + 1 temperature sensor + 2 number configuration tiles + 1 mode select); 5 MANDATORY §7 safety interlocks (leak detected via ANY leak sensor (under-sink / pump-area / under-van) stopping the pump + sending a HIGH-PRIORITY push notification; freeze risk via `sensor.rc_water_fresh_temperature_c` < 2 °C cross-referencing the heated-floors + hvac-basics connections' frost-warning path; fresh empty warning via `sensor.rc_water_fresh_level_pct` < 5 % surfacing prominently on the dashboard; pump running too long via `binary_sensor.rc_water_pump_running` continuously TRUE for > 10 min auto-stopping the pump; mode-aware lockouts (Stealth auto-mute warnings / Sleep silent / Boost disable-mode-aware-lockouts) via `select.rc_mode`); 6 §8 automations (auto-push on fresh low / auto-push on grey full / auto-stop pump when pump running too long / auto-push critical on leak detected / auto-engage heated-floors on freeze risk when SOC > 50 % / mode-aware scheduling); 8 §9 troubleshooting entries (sensor reading 0 % when tank is full / sensor reading 100 % when tank is empty / pump_running not toggling / leak sensor always-on / ESPHome device offline / Shelly UNI not discovered / temperature reading -40 °C / fresh_days_remaining negative); §10 privacy (no telemetry beyond local level + pump runtime + temperature; no vendor cloud / no ESPHome cloud / no Shelly cloud / no leak-sensor cloud); §11 tier-a promotion outline (real fresh tank + grey tank + pump + leak sensor + temperature probe + ESP32 + ultrasonic probe + CT clamp + Shelly UNI bench on CI + RoamCore-owned config_flow.py walking Path A vs Path B vs Path C + GPIO pin / calibration curve / vendor integration declaration + integration tests asserting a 0 % → 100 % level change triggers the right tile updates + the 5 safety interlocks all flip when wired to canned fixture responses))
  - manifest-honesty smoke: `connections/water-tanks/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_safety_interlocks_are_documented` defensive guard asserting all 5 safety interlocks (leak detected / freeze risk / fresh empty / pump running too long / mode-aware lockouts) are documented in recipe §7 + cross-reference `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` from Victron + the heated-floors Wave 3 #44 companion + the hvac-basics Wave 3 #49 companion + `select.rc_mode` from mode/automation-builder + the "MANDATORY before first use" emphasis in recipe §7)
  - 17 contract entities all `rc_water_*`: 5 sensor telemetry tiles (`sensor.rc_water_fresh_level_pct` / `sensor.rc_water_fresh_level_l` / `sensor.rc_water_fresh_days_remaining` / `sensor.rc_water_grey_level_pct` / `sensor.rc_water_grey_level_l`) + 7 binary_sensor warning tiles (`binary_sensor.rc_water_grey_full_warning` / `binary_sensor.rc_water_fresh_low_warning` / `binary_sensor.rc_water_fresh_empty_warning` / `binary_sensor.rc_water_pump_running` / `binary_sensor.rc_water_pump_running_too_long` / `binary_sensor.rc_water_leak_detected` / `binary_sensor.rc_water_freeze_risk`) + 1 temperature sensor tile (`sensor.rc_water_fresh_temperature_c`) + 2 number configuration tiles (`number.rc_water_fresh_tank_size_l` / `number.rc_water_grey_tank_size_l`) + 1 mode select tile (`select.rc_water_mode`) (per docs/reference/rc-entity-naming.md §water subsystem — the `water` subsystem was added to the allowed subsystems list alongside this slice, mirroring how `media` was added by the Music Assistant slice + how `presence` was backfilled alongside the bluetooth-wifi-presence slice + how `bed_lift` was added alongside the happijac slice + how `hvac` was added alongside the heated-floors slice + how `safety` was added alongside the smoke-co-gas-sensors slice)
  - vendor-neutrality strictly enforced: NO `seelevel`, `see_level`, `mopeka`, `lippert`, `garnet`, `esphome`, `shelly`, `ads1115`, `jsnsr04t`, `hc_sr04`, `ds18b20`, `sct013`, `sct_013`, `current`, `adc`, `voltage`, `sensor_`, `binary_sensor_` appears in any tile id BEYOND the `rc_water_*` subsystem prefix (the spec-required tile IDs legitimately contain `fresh`, `grey`, `level`, `pump`, `runtime`, `leak`, `freeze`, `temperature`, `tank`, `size`, `mode`, `warning`, `empty`, `low`, `full`, `days`, `remaining`, `too_long`, `min`, `24h` in the suffix to describe what the tile is for — these are the generic-noun tolerance list per spec; the forbidden substrings target the vendor-name / hardware-name / sensor-model / measurement-unit absolute-forbidden set only)
  - legacy tier-c catalog page (`docs/catalog/water/water-tanks.md`) now carries a supersession banner pointing at the new connection folder; the original "Support tier: C (manual / varies)" placeholder was retired (no recipe + no contract + no automations — just a "add your preferred sensors/integration" placeholder — now superseded by the tier-b recipe connection with a 17-tile vendor-neutral contract + 11-section howto + 5 safety interlocks + 6 automations + 8 troubleshooting entries)
  - cross-reference to Heated floors + engine pre-heat: §7.2 freeze risk auto-engages the heated-floors Wave 3 #44 recipe §7.5 frost-protection automation when `binary_sensor.rc_water_freeze_risk` transitions to TRUE AND `sensor.rc_power_battery_soc` > 50 % — the water-tanks freeze_risk tile is the third pillar of the cold-weather safety story (frozen fresh water tank + frozen pipes = cracked tank + burst pipes + no drinking water + no shower water)
  - cross-reference to HVAC basics: §7.2 freeze risk cross-references the hvac-basics Wave 3 #49 recipe §7.1 frost-warning which keeps the cabin thermostat > 5 °C — the water-tanks + hvac-basics + heated-floors frost-warning triad is the cold-weather safety net for the van
  - cross-reference to Victron: §7.4 pump running too long auto-stop pattern uses `sensor.rc_power_battery_soc` + `binary_sensor.rc_power_shore_connected` from the Victron `connections/victron/` recipe — the water pump can pull 5–10 A sustained when running; below 30 % SOC + shore disconnected, the auto-stop prevents the pump from draining the battery bank
  - cross-reference to Smoke / CO / gas sensors: §7.1 leak detected automation uses the same "loud siren + phone notification on any alarm" pattern as the smoke-co-gas-sensors Wave 3 #45 recipe §7.1 — the leak detected push notification is HIGH-PRIORITY because leaks in a van ruin everything (water + electronics + cabinetry + insulation all die together)
  - cross-reference to Deadbolts: §7.1 leak detected automation cross-references the deadbolts Wave 3 #48 recipe's §7.x emergency-egress unlock — the operator must be able to get OUT of the van even if water is pooling near the door
  - cross-reference to Mode / automation-builder: §7.5 mode-aware lockouts use `select.rc_mode` from the mode/automation-builder `connections/mode-automation-builder/` recipe for the Stealth auto-mute warnings + Sleep silent + Boost disable-mode-aware-lockouts
  - PR #51

## Next steps (needs HAOS setup / UI wiring)

1) **Setup Wizard dashboard**
   - Add a Lovelace dashboard YAML for setup flow.
   - Wire stubs to OpenWrt API + Victron connect UI.

2) **Traccar install + integration in HAOS**
   - Install Traccar add-on (or point to external).
   - Configure HA Traccar integration so `device_tracker.*` exists.
   - Set `input_text.rc_location_tracker_entity` to the correct entity.

3) **Trip stats (rc_trip_*) from real Traccar data**
   - MVP still uses mocks for distance/time/stops.
   - Implement: odometer-based + utility_meter or periodic report pulls.

4) **HACS packaging (planned)**
   - Publish a HACS integration to install RoamCore from the HA UI.
   - Auto-create dashboard + resources.
