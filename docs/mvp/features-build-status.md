# RoamCore MVP — Features Build Status

Last updated: 2026-08-02

This is an internal status page for the remaining MVP feature build-out.

## Shipped (repo)

- Weather + time contract sensors
  - `homeassistant/packages/roamcore_weather_time.yaml`

- Timezone override contract sensor (no HA restart required)
  - `sensor.rc_time_zone` via `input_text.rc_time_zone_override`

- Levelling contract (HA-only beta)
  - `homeassistant/packages/roamcore_level.yaml`
  - auto-maps common ESPHome pitch/roll sensors into stable `rc_level_*` entities

- Map view wiring
  - `dashboard/lovelace/storage/lovelace.roamcore.json` includes `/lovelace/roamcore/map`
  - `homeassistant/packages/roamcore_location.yaml` maps a configurable `device_tracker` → `rc_location_*`

- Trip Wrapped (MVP HTML export)
  - tool: `homeassistant/tools/trip_wrapped/`
  - HA wiring: `homeassistant/packages/roamcore_trip_wrapped.yaml`
  - output: `/local/roamcore/trip_wrapped/latest.html`

- OpenClaw JSON API (HA-native)
  - endpoint: `/api/roamcore/openclaw/summary`
  - docs: `docs/reference/openclaw-json-api.md`

- Traccar live map (embedded)
  - RoamCore Map page embeds Traccar add-on **web UI** via iframe (configurable).
  - Helper: `input_text.rc_traccar_ui_url`

- NFC tags (vendor-neutral NFC-triggered scenes mapped via `tag_id → scene` mapping) (tier-c recipe connection)
  - tier-c manifest: `connections/nfc-tags/connection.yml` (access_control category, recipe_published status; reuse-first recipe over upstream HA core `tag` integration (since 2022.x — exposes a `tag_scanned` event + a `tag.last_scanned` entity + a `tag.list` service) + the HA Companion app's `tag` trigger (Path A phone-as-NFC-reader + Path C implicit Path A) + the HACS `nfcpy` integration (Path B USB NFC reader for ACR122U / PN532 / SonMicro / Identiv) + the HA core `scene` integration (the operator's `scene.*` entities) + the HA core `automation` UI editor (the operator-wired setup flow for the `tag_id → scene` mapping table) + a thin RoamCore automation wrapper; RoamCore does NOT maintain a custom NFC integration; `install.hacs: true` + `install.config_flow: true` because the recipe optionally depends on the HACS `nfcpy` integration + the upstream HA core `tag` integration + the HA core `scene` integration + the HA core `automation` UI editor + the HA Companion app's `tag` trigger all expose a GUI flow since 2022.x — honest upstream truth, NOT a tier-a marker for RoamCore's tier; THREE operator-pickable paths — Path A phone-as-NFC-reader via the HA Companion app (default for any van operator with a modern Android phone with NFC built in); Path B USB NFC reader via the HACS `nfcpy` integration (default for vans where the operator uses an iPhone OR for vans where the operator wants a SINGLE shared NFC reader that everyone in the van can use); Path C implicit Path A via the HA Companion app's `tag` trigger (functionally identical to Path A; the recipe treats Path C as an "implicit" Path A))
  - recipe: `connections/nfc-tags/docs/recipe.md` (~800+ line howto: HA core `tag` integration install + HA Companion app NFC scan setup + HACS `nfcpy` integration setup + Path A phone-as-NFC-reader wiring (7 steps: install the HA Companion app + connect to HA instance + enable NFC scans + test the NFC scan + register the NFC tag in HA's tag registry + create the operator's scenes in HA's scene registry + wire the §7.1 last-tag-triggered scene automation with the `tag_id → scene` mapping table) + Path B USB NFC reader wiring (7 steps: plug the USB NFC reader into the HA server + install the HACS `nfcpy` integration + configure the HACS `nfcpy` integration + test the NFC scan + register the NFC tag + create the scenes + wire the §7.1 automation) + Path C implicit Path A wiring (7 steps: install the HA Companion app + connect to HA instance + enable the HA Companion app's `tag` trigger + test the NFC scan + register the NFC tag + create the scenes + wire the §7.1 automation) + the §7.1 last-tag-triggered scene automation (the `tag_id → scene` mapping table — the operator's single source of truth for which tag fires which scene) + the §7.2 tag-unknown warning automation (the operator's on-ramp for adding new tags — sends a notification to the operator's phone via the HA Companion app saying "Unknown NFC tag scanned: <tag_id> — register it in the RoamCore tag_id → scene mapping table") + the §7.3 Stealth-mode suppression automation (the operator's quiet-campground-aware affordance — SUPPRESSES the §7.1 last-tag-triggered scene automation when the mode/automation-builder recipe's `select.rc_mode` tile is in `stealth` mode) + the 8 `rc_nfc_*` contract tiles (4 sensor + 3 binary_sensor + 1 button — last triggered scene + last triggered scene active + registered tags count + last scanned tag id + last scan minutes ago + tag unknown warning + stealth mode suppressed + trigger scene now); 3 MANDATORY §7 automations (last-tag-triggered scene + tag-unknown warning + Stealth-mode suppression); 6 §8 troubleshooting entries (NFC scan event fires but no scene is triggered / NFC scan event DOES NOT fire when I tap a tag / re-scanning the same tag fires the same scene repeatedly / tag-unknown warning fires for a tag I just registered / Stealth-mode suppression is always active / last-tag-triggered scene fires but the scene does NOT do anything); §9 privacy (no telemetry beyond local HA tag registry + scene registry + scan event log; all processing is local to the HA instance; no cloud call home; Path A / Path C HA Companion app's NFC sensor is opt-in; Path B HACS `nfcpy` integration is operator-installed; operator's `tag_id → scene` mapping table is operator-owned); §10 tier-b promotion outline (real NFC bench on CI + RoamCore-owned operator-wired setup flow + integration tests asserting a `tag_scanned` event with a known `tag_id` triggers the mapped scene + a `tag_scanned` event with an unknown `tag_id` fires the tag-unknown warning + a `select.rc_mode` change to `stealth` suppresses the §7.1 automation))
  - manifest-honesty smoke: `connections/nfc-tags/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_tier_c_documents_reuse_first_strategy` defensive guard asserting tier=c + `wizard.one_tap=false` + `install.config_flow=true` honest because the UPSTREAM HA core `tag` integration + the HA core `scene` integration + the HA core `automation` UI editor + the HA Companion app's `tag` trigger + the HACS `nfcpy` integration all expose a GUI flow since 2022.x + `install.hacs=true` because NFC tags optionally depends on the HACS `nfcpy` integration + NO RoamCore-owned `config_flow.py` + DOMAIN=`nfc_tags` + the description explicitly documents the reuse-first strategy over the upstream HA core `tag` integration + the links.official list includes the HA core `tag` integration upstream doc URL; the `test_dashboard_tiles_follow_rc_naming` defensive guard asserting 8 vendor-neutral `rc_nfc_*` tiles (NOT `rc_nfc_tag_*` and NOT `rc_nfc_scene_*` and NOT `rc_access_control_*` — the NFC tags prefix is the SPECIFIC NFC subsystem subset of the broader access_control category, mirroring how time-atomic Wave 3 #55 inherits the `rc_time_*` prefix from the existing time helpers and how in-cab-tablet-dashboard Wave 3 #56 inherits the `rc_in_cab_tablet_*` prefix from the existing Wican Pro entities) + forbidden_substrings covers vendor + hardware + protocol + integration names including `acr122u`, `pn532`, `sonmicro`, `identiv`, `nfcpy`, `ntag215`, `ntag216`, `mifare`, `ultralight`, `sticker`, `nfc_reader`, `iso14443`, `iso15693`, `nfcforum`, `ndef`, `hacs`, `hass`, `ha_integration`, `ha_companion`, `mqtt`, `esphome`, `esp32`, `traccar`, `wican`, `obd`, `frigate`, `homeassistant`, `device_tracker`, `set_location`, `update_entity` (but preserves the spec-required `tag_id` substring in the `sensor.rc_nfc_last_scanned_tag_id` tile id and the `scene` substring in the `sensor.rc_nfc_last_triggered_scene` tile id); the `test_status_reflects_no_real_nfc_integration` defensive guard asserting status=recipe_published + 5 tier_warnings present (`no_native_nfc_integration` + `recipe_depends_on_user_registering_nfc_tags_in_ha_registry` + `requires_operator_choice_of_path_a_phone_nfc_reader_or_path_b_usb_nfc_reader_or_path_c_implicit` + `no_real_nfc_reader_hardware_on_ci_bench` + `mode_aware_stealth_suppression_required_when_used_at_campgrounds`); the `test_automations_are_documented` defensive guard asserting the THREE §7 automations documented (last-tag-triggered scene + tag-unknown warning + Stealth-mode suppression) + 4 safety tiles wired (`sensor.rc_nfc_last_triggered_scene` + `binary_sensor.rc_nfc_tag_unknown_warning` + `binary_sensor.rc_nfc_stealth_mode_suppressed` + `button.rc_nfc_trigger_scene_now`) + cross-references to HA core `tag` integration + home-assistant.io/integrations/tag + HA Companion app + HACS `nfcpy` integration + HA core `scene` integration + mode/automation-builder recipe's `select.rc_mode` tile + deadbolts Wave 3 #48 + approach-lights Wave 3 #52 + HVAC basics Wave 3 #49)
  - 8 contract entities all `rc_nfc_*`: 4 sensor tiles (`sensor.rc_nfc_last_triggered_scene` / `sensor.rc_nfc_registered_tags_count` / `sensor.rc_nfc_last_scanned_tag_id` / `sensor.rc_nfc_last_scan_minutes_ago`) + 3 binary_sensor tiles (`binary_sensor.rc_nfc_last_triggered_scene_active` / `binary_sensor.rc_nfc_tag_unknown_warning` / `binary_sensor.rc_nfc_stealth_mode_suppressed`) + 1 button tile (`button.rc_nfc_trigger_scene_now`) (per docs/reference/rc-entity-naming.md §nfc subsystem — the `nfc` subsystem is OWNED by this slice; the `nfc` subsystem was added by this slice to the Allowed subsystems list in `docs/reference/rc-entity-naming.md`; the spec-required tile IDs legitimately contain `scene`, `tag_id`, `active`, `registered`, `count`, `last`, `scanned`, `id`, `minutes`, `ago`, `warning`, `unknown`, `stealth`, `mode`, `suppressed`, `trigger`, `now` in the suffix to describe what the tile is for — these are the generic-noun tolerance list per spec; the forbidden substrings target the vendor-name / hardware-name / protocol-name / integration-name absolute-forbidden set only; the tile prefix is `rc_nfc_*` — NOT `rc_nfc_tag_*` and NOT `rc_nfc_scene_*` and NOT `rc_access_control_*` — the access_control category is the canonical category for NFC tags + the deadbolts Wave 3 #48 connection's `rc_access_control_*` prefix; the NFC tags connection uses the `rc_nfc_*` prefix because `nfc` is the canonical NFC subsystem)
  - vendor-neutrality strictly enforced: NO `acr122u`, `pn532`, `sonmicro`, `identiv`, `nfcpy`, `ntag215`, `ntag216`, `mifare`, `ultralight`, `sticker`, `nfc_reader`, `iso14443`, `iso15693`, `nfcforum`, `ndef`, `ntag`, `nfcpy_`, `hacs`, `hass`, `ha_integration`, `ha_companion`, `mqtt`, `esphome`, `esp32`, `traccar`, `wican`, `obd`, `frigate`, `homeassistant`, `device_tracker`, `set_location`, `update_entity` appears in any rc_* tile id BEYOND the `rc_nfc_*` subsystem prefix (the spec-required tile ID `sensor.rc_nfc_last_scanned_tag_id` legitimately contains `tag_id` in the suffix to describe the operator's most recent scanned tag ID — this is the OPERATIONAL concept, NOT a vendor leak; the forbidden substrings `tag_id_` and `tag_id-` target the upstream tag attribute surface only; the spec-required tile ID `sensor.rc_nfc_last_triggered_scene` legitimately contains `scene` in the suffix because the contract surface is about scenes — the §7.1 automation calls `scene.turn_on` on the mapped scene)
  - legacy tier-c catalog page (`docs/catalog/nfc-tags/index.md`) now carries a supersession banner pointing at the new connection folder
  - cross-reference to HA Companion app: §3 Path A phone-as-NFC-reader wiring uses the HA Companion app's NFC sensor (since 2022.x — the phone-side NFC scan event that fires a `tag_scanned` event in HA core when the operator taps an NFC tag to the phone)
  - cross-reference to HACS `nfcpy` integration: §4 Path B USB NFC reader wiring uses the HACS `nfcpy` integration (HACS — the canonical USB NFC reader driver for ACR122U / PN532 / SonMicro / Identiv)
  - cross-reference to HA core `scene` integration: §3 / §4 / §5 scene registry wiring uses the HA core `scene` integration (the operator's `scene.*` entities; since 2022.x)
  - cross-reference to mode/automation-builder recipe: §7.3 Stealth-mode suppression automation uses the mode/automation-builder recipe's `select.rc_mode` tile (the source of truth for the Stealth-mode suppression; Wave 2 #23)
  - cross-reference to deadbolts (optional): the recipe's §11 cross-references the deadbolts Wave 3 #48 connection for the optional "tag-unlock-the-door" affordance that uses the same `tag_id → scene` mapping pattern
  - cross-reference to Approach lights (optional): the recipe's §11 cross-references the approach-lights Wave 3 #52 connection for the canonical "Lights off" / "Welcome home" scene entities that the `tag_id → scene` mapping table can use as scene targets
  - cross-reference to HVAC basics (optional): the recipe's §11 cross-references the HVAC basics Wave 3 #49 connection for the canonical "Bedtime" / "Climate" scene entities that the `tag_id → scene` mapping table can use as scene targets
  - PR #61

## Next steps (needs HAOS setup / UI wiring)

1) **Setup Wizard dashboard**
   - Add a Lovelace dashboard YAML for setup flow.
   - Wire stubs to OpenWrt API + Victron connect UI.

2) **Traccar install + integration in HAOS**
   - Install Traccar add-on (or point to external).
   - Configure HA Traccar integration so `device_tracker.*` exists.
   - Set `input_text.rc_location_tracker_entity` to the correct entity.

3) **Trip stats (rc_trip_*) from real Traccar data**
   - MVP still uses mocks for distance/time/stops.
   - Implement: odometer-based + utility_meter or periodic report pulls.

4) **HACS packaging (planned)**
   - Publish a HACS integration to install RoamCore from the HA UI.
   - Auto-create dashboard + resources.
