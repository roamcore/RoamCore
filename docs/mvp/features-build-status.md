# RoamCore MVP — Features Build Status

Last updated: 2026-08-03

This is an internal status page for the remaining MVP feature build-out.

## Shipped (repo)

- Weather + time contract sensors
  - `homeassistant/packages/roamcore_weather_time.yaml`

- Timezone override contract sensor (no HA restart required)
  - `sensor.rc_time_zone` via `input_text.rc_time_zone_override`

- Levelling contract (HA-only beta)
  - `homeassistant/packages/roamcore_level.yaml`
  - auto-maps common ESPHome pitch/roll sensors into stable `rc_level_*` entities

- Advanced mode (power-user toggle that reveals extra controls + diagnostics without cluttering the default UI) (tier-b recipe connection)
  - tier-b manifest: `connections/advanced-mode/connection.yml` (ai category, beta status; reuse-first recipe over upstream HA core `input_boolean` + `input_text` + `input_datetime` + `input_button` + `select` helpers (since 2022.x — expose a GUI flow for the operator to add the helper entities from the HA UI under Settings → Helpers) + HA core `template:` sensor wrapper (since 2022.x — wraps any upstream sensor state into a derived `sensor.*` entity) + HA core `template:` binary_sensor wrapper (since 2022.x — wraps any upstream sensor threshold into a derived `binary_sensor.*` entity) + a thin RoamCore upstream-entity-aggregation wrapper; RoamCore does NOT maintain a custom advanced-mode engine; the upstream helpers + `template:` wrappers handle 95%+ of operator-facing advanced-mode operations; `install.hacs: false` + `install.config_flow: true` because the recipe depends on the HA core `input_boolean` + `input_text` + `input_datetime` + `input_button` + `select` helpers + the HA core `template:` sensor + `template:` binary_sensor wrappers all expose a GUI flow since 2022.x — honest upstream truth, NOT a tier-a marker for RoamCore's tier; FOUR-step operator flow — Step 1 Confirm (operator flips `input_boolean.rc_advanced_mode_confirmed` ON, acknowledging "I understand advanced mode exposes destructive irreversible service calls"); Step 2 Enable (operator flips `input_boolean.rc_advanced_mode_enabled` ON OR presses `button.rc_advanced_mode_enable` — flips the master toggle ON + sets `input_datetime.rc_advanced_mode_session_expires_at` to "now + selected duration" + surfaces a confirm-modal if the confirm-flag is OFF); Step 3 Session window (advanced mode stays ON until either the operator disables it OR the session_expires_at timestamp is reached; default 24 hours, controlled by the `select.rc_advanced_mode_session_duration` selector 1 hour / 6 hours / 24 hours / 7 days / Never); Step 4 Audit + revert (every destructive irreversible service call the operator initiates is logged to `sensor.rc_advanced_mode_last_action_at` + counted in `sensor.rc_advanced_mode_session_action_count`; the operator can revert at any time via `button.rc_advanced_mode_disable_now`))
  - recipe: `connections/advanced-mode/docs/recipe.md` (~1035-line howto: HA core `input_boolean` + `input_text` + `input_datetime` + `input_button` + `select` helpers install + HA core `template:` sensor wrapper install + HA core `template:` binary_sensor wrapper install + §3 Step 1 Confirm wiring (5 steps) + §4 Step 2 Enable wiring (8 steps) + §5 Step 3 Session window wiring (6 steps) + §6 Step 4 Audit + revert wiring (5 steps) + §7 the 11 `rc_advanced_mode_*` contract tiles (1 input_boolean confirmed + 1 input_boolean enabled + 1 input_datetime session_expires_at + 1 sensor seconds_until_expiry + 1 sensor session_action_count + 1 sensor last_action_at + 1 binary_sensor is_active + 1 binary_sensor is_blocking_destructive_calls + 1 select session_duration + 1 button enable + 1 button disable_now = 11 contract entities); FIVE MANDATORY §8 automations (§8.1 confirm-before-toggle-on guard (triggers when a non-operator source tries to flip `input_boolean.rc_advanced_mode_enabled` ON without first flipping `input_boolean.rc_advanced_mode_confirmed` ON; BLOCKS the enable flip + writes an audit-log entry + fires a notification warning the operator that the confirm-flag must be flipped ON first) + §8.2 auto-disable after session timeout (triggers when `input_datetime.rc_advanced_mode_session_expires_at` timestamp is reached; clears the enable toggle + clears the session_expires_at + resets the session_action_count to 0 + clears the last_action_at to unknown + writes an audit-log entry + fires a notification warning the operator that advanced mode has been auto-disabled) + §8.3 hides-for-non-owners guard (triggers when a non-owner dashboard session attempts to view the advanced-mode dashboard page while `input_boolean.rc_advanced_mode_enabled` is ON; hides the advanced-mode tiles + surfaces an "advanced mode hidden for non-owners" banner + writes an audit-log entry) + §8.4 audit-log entry on destructive call (triggers on every destructive irreversible service call the operator initiates while `input_boolean.rc_advanced_mode_enabled` is ON; writes an audit-log entry with the service name + the target entity + the operator identity (if the remote-access session tracks it) + the timestamp + the reason) + §8.5 blocks-destructive-irreversible-service-calls guard (triggers when ANY `script.*` / `automation.*` action tries to call a destructive irreversible service (the operator has flagged in `input_text.rc_advanced_mode_destructive_call_targets`) while `input_boolean.rc_advanced_mode_enabled` is OFF; BLOCKS the service call + logs a security-style audit entry + flips `binary_sensor.rc_advanced_mode_is_blocking_destructive_calls` to FALSE + fires a critical notification)); 6 §9 troubleshooting entries (advanced mode is stuck on Confirmed but not Enabled + advanced mode never surfaces the hidden controls + §8.2 auto-disable after session timeout guard never fires + §8.5 blocks-destructive-irreversible-service-calls guard fires unexpectedly + §8.3 hides-for-non-owners guard surfaces banner every time + §8.1 confirm-before-toggle-on guard BLOCKS the operator's own enable); §10 privacy (no RoamCore-side telemetry; the upstream helper entities' logs are operator-owned via the HA core logbook; advanced-mode values are clearly labelled as advanced + never reach the operator's default automations); §11 tier-a promotion outline (real advanced-mode engine + canned fixture responses for session-expired events on CI bench + canned fixture responses for destructive-service-call blocking events + canned fixture responses for non-owner dashboard session events + RoamCore-owned operator-wired setup flow walking the operator through Confirm + Enable + Session window + Audit + declare the upstream destructive irreversible service call targets + the §8 automations + integration tests asserting a session-expired event auto-disables advanced mode + a destructive-service-call attempt on a flagged target triggers the blocks-destructive-irreversible-service-calls guard + a non-owner dashboard session triggers the hides-for-non-owners guard))
  - manifest-honesty smoke: `connections/advanced-mode/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_tier_b_without_tier_a_markers` defensive guard asserting tier=b + `wizard.one_tap=false` + `install.config_flow=true` honest because the UPSTREAM HA core `input_boolean` + `input_text` + `input_datetime` + `input_button` + `select` helpers + the HA core `template:` sensor + `template:` binary_sensor wrappers all expose a GUI flow since 2022.x + `install.hacs=false` because advanced-mode does NOT depend on a HACS add-on as a required dependency + NO RoamCore-owned `config_flow.py` + DOMAIN=`advanced_mode` + the description explicitly documents the reuse-first strategy over the upstream HA core `input_boolean` + `input_text` + `input_datetime` + `input_button` + `select` helpers + the HA core `template:` sensor + `template:` binary_sensor wrappers + the links.official list includes the HA core `input_boolean` integration upstream doc URL + the substring guard rephrasing check (the docstring contains `operator-wired` + `GUI flow` to avoid the literal `config_flow.py` substring trap); the `test_dashboard_tiles_follow_rc_naming` defensive guard asserting exactly 11 vendor-neutral `rc_advanced_mode_*` tiles (NOT `rc_victron_*` and NOT `rc_see_level_*` and NOT `rc_seelevel_*` and NOT `rc_garnet_*` and NOT `rc_mopeka_*` and NOT `rc_renogy_*` and NOT `rc_starlink_*` and NOT `rc_peplink_*` and NOT `rc_teltonika_*` and NOT `rc_unifi_*` and NOT `rc_ubiquiti_*` and NOT `rc_input_boolean_*` and NOT `rc_input_text_*` and NOT `rc_input_datetime_*` and NOT `rc_input_button_*` and NOT `rc_select_*` and NOT `rc_template_*`) + forbidden_substrings covers vendor + hardware + protocol + integration names including `victron`, `renogy`, `shunt`, `bms`, `inverter`, `mppt`, `see level`, `seelevel`, `garnet`, `mopeka`, `icon`, `resistive`, `tank`, `starlink`, `peplink`, `teltonika`, `unifi`, `ubiquiti`, `mqtt`, `webhook`, `rest`, `api`, `http`, `https`, `ha core`, `ha_`, `hacs`, `tasmota`, `esphome`, `companion`, `esp32`, `esp8266`, `nodemcu`, `wemos`, `shelly`, `sonoff`, `zwave`, `zha`, `zigbee`, `deconz`, `conbee`, `raspbee`, `nous`, `aqara`, `bluetooth`, `wifi`, `wi-fi`, `input_boolean`, `input_text`, `input_datetime`, `input_button`, `gps`, `accelerometer`, `gyroscope`, `magnetometer`, `compass`, `heading`, `iphone`, `ios`, `android`, `samsung`, `pixel`, `oneplus`, `xiaomi`, `huawei`, `phone` (note: `ble`, `ble`, `lte`, `router`, `cellular`, `select` deliberately omitted from forbidden_substrings because they're legitimate generic nouns for the advanced-mode contract — `select` is the modern `select:` domain helper that's too short for substring matching; the audit catches true BLE / vendor leaks via the longer `bluetooth` / `starlink` / `peplink` / `teltonika` / `unifi` / `ubiquiti` substrings); the `test_status_reflects_no_native_advanced_mode_engine` defensive guard asserting status=beta + 5 tier_warnings present (`no_native_advanced_mode_engine_for_integration_test` + `recipe_depends_on_user_wiring_session_duration_default` + `recipe_depends_on_user_declaring_destructive_irreversible_service_call_targets` + `requires_operator_wiring_confirm_flag_before_first_use` + `advanced_mode_blocks_destructive_irreversible_calls_guard_must_be_wired`); the `test_automations_are_documented` defensive guard asserting the FIVE §8 MANDATORY automations documented (confirm-before-toggle-on guard + auto-disable after session timeout + hides-for-non-owners guard + audit-log entry + blocks-destructive-irreversible-service-calls guard) + 5 safety tiles wired (`input_boolean.rc_advanced_mode_confirmed` + `input_boolean.rc_advanced_mode_enabled` + `input_datetime.rc_advanced_mode_session_expires_at` + `binary_sensor.rc_advanced_mode_is_blocking_destructive_calls` + `button.rc_advanced_mode_disable_now`) + cross-references to time-atomic Wave 3 #55 + home-assistant.io/integrations/input_boolean + template + binary_sensor (HA core `template:` binary_sensor wrapper) + remote-access Wave 3 #58 + mode Wave 3 #61 + demo-mode Wave 3 #62 + leveling Wave 3 #60 + fans Wave 3 #59)
  - vendor-neutrality: 11 `rc_advanced_mode_*` tiles are vendor-neutral — NO Victron / SeeLevel / Seelevel / Garnet / Mopeka / Renogy / Starlink / Peplink / Teltonika / Unifi / Ubiquiti / MQTT / webhook / REST / API / HTTP / HTTPS / input_boolean / input_text / input_datetime / input_button / select / template / Companion / ESPHome / phone / GPS / accelerometer / gyroscope / magnetometer / compass / heading / iPhone / iOS / Android / Samsung / Pixel / OnePlus / Xiaomi / Huawei names leak into the tile ids
  - legacy tier-a-claim catalog page (`docs/catalog/ai/advanced-mode.md`) now carries the SUPERSEDED banner pointing at the new connection folder (the legacy tier-a "RoamCore native" claim is honest-upstream-truth: RoamCore ships no native advanced-mode engine today)
  - legacy package preserved (not touched): `homeassistant/packages/roamcore_advanced_mode.yaml`
  - cross-reference to HA core `input_boolean` integration: §7 `input_boolean.rc_advanced_mode_confirmed` + `input_boolean.rc_advanced_mode_enabled` storage uses the HA core `input_boolean` integration
  - cross-reference to HA core `input_text` integration: §7 `input_text.rc_advanced_mode_destructive_call_targets` storage uses the HA core `input_text` integration
  - cross-reference to HA core `input_datetime` integration: §7 `input_datetime.rc_advanced_mode_session_expires_at` storage uses the HA core `input_datetime` integration
  - cross-reference to HA core `input_button` integration: §7 `button.rc_advanced_mode_enable` + `button.rc_advanced_mode_disable_now` storage uses the HA core `input_button` integration
  - cross-reference to HA core `select` integration: §7 `select.rc_advanced_mode_session_duration` storage uses the HA core `select` integration (since 2024.x)
  - cross-reference to HA core `template:` sensor wrapper: §7 seconds-until-expiry + session-action-count + last-action-at derivation uses the HA core `template:` sensor wrapper
  - cross-reference to HA core `template:` binary_sensor wrapper: §7 is-active + is-blocking-destructive-calls derivation uses the HA core `template:` binary_sensor wrapper
  - cross-reference to time-atomic Wave 3 #55: §8.2 auto-disable after session timeout guard's expiry timestamp
  - cross-reference to remote-access Wave 3 #58: §8.3 hides-for-non-owners guard's VPN primitive
  - cross-reference to mode Wave 3 #61: §8.4 audit-log entry's mode-change cross-reference
  - cross-reference to demo-mode Wave 3 #62: §8.1 confirm-before-toggle-on guard's confirm-flag pattern
  - cross-reference to leveling Wave 3 #60: §8.5 blocks-destructive-irreversible-service-calls guard's levelling-jack protection cross-reference
  - cross-reference to fans Wave 3 #59: §8.5 blocks-destructive-irreversible-service-calls guard's fan-protection cross-reference
  - new `advanced_mode` subsystem added to `docs/reference/rc-entity-naming.md` (the `advanced_mode` subsystem is OWNED by this slice — the FIRST `ai`-category `advanced_mode` slice in the RoamCore connection pipeline; back-fills `ventilation` + `vehicle` + `mode` + `demo_mode` subsystems since all four were missing from the Allowed subsystems list on the fresh-from-main branch cut)
  - Last updated: 2026-08-03
  - PR #67

- Amenities overlay (vendor-neutral iOverlander-style POI overlay on the RoamCore Map page + offline cache fallback + 8 POI categories + 5 §8 MANDATORY automations) (tier-b recipe connection)
  - tier-b manifest: `connections/amenities-overlay/connection.yml` (map category, beta status; reuse-first recipe over the upstream Overpass API (`https://overpass-api.de/api/interpreter` — an OpenStreetMap project, fully open + free + community-run since 2013; iOverlander itself uses Overpass for its amenity queries) + the HA core `rest:` integration (since 2017) + the HA core `input_boolean` + `input_select` + `input_number` helpers (since 2022.x — expose a GUI flow under Settings → Helpers) + the HA core `template:` sensor wrapper (since 2022.x) + the HA core `template:` binary_sensor wrapper (since 2022.x) + the optional RoamCore TileServer add-on (Wave 2 catalog #21 — serves pre-cached POI mbtiles for offline use) + a thin RoamCore upstream-entity-aggregation wrapper; RoamCore does NOT maintain a custom amenities-overlay engine; the upstream Overpass API + HA core `rest:` + HA core `input_boolean` + HA core `input_select` + HA core `input_number` + HA core `template:` sensor + HA core `template:` binary_sensor + the optional RoamCore TileServer add-on together handle 95%+ of operator-facing amenities-overlay operations; `install.hacs: false` + `install.config_flow: true` because the recipe depends on the HA core `input_boolean` + `input_select` + `input_number` helpers + the `rest:` integration + the `template:` sensor + `template:` binary_sensor wrappers all expose a GUI flow since 2017/2022.x — honest upstream truth, NOT a tier-a marker for RoamCore's tier; FIVE-step operator flow — Step 1 Confirm-reachability (operator curls `https://overpass-api.de/api/interpreter` with a minimal `amenity=drinking_water` query to verify reachability from the HA host); Step 2 Pick-categories (operator flips the 8 per-category `enable_*` buttons ON for the POI categories they care about: water + laundry + gym + dump_point + campsite + wild_camping + supermarket + fuel = 8 categories total); Step 3 Pick-radius (operator picks 1 / 2 / 5 / 10 / 25 / 50 km via `select.rc_amenities_overlay_radius_km` — default 5 km); Step 4 Pick-data-source (operator picks Overpass / Offline cache / Auto via `select.rc_amenities_overlay_data_source` — "Auto" tries Overpass first, falls back to the offline cache if Overpass rate-limits or is unreachable); Step 5 Enable + configure cache-TTL + rate-limit (operator flips `input_boolean.rc_amenities_overlay_enabled` ON + sets `number.rc_amenities_overlay_cache_ttl_min` (5–1440 min; default 60) + sets `number.rc_amenities_overlay_rate_limit_per_hour` (1–100; default 30) + ensures `input_boolean.rc_amenities_overlay_fail_safe` is ON for the §8.1 guard)
  - recipe: `connections/amenities-overlay/docs/recipe.md` (~1373-line howto: Overpass API reachability verify + HA core `rest:` install + HA core `input_boolean` + `input_select` + `input_number` helpers install + HA core `template:` sensor + `template:` binary_sensor wrappers install + RoamCore TileServer add-on (Wave 2 #21) optional install + §3 the 8 operator-pickable POI categories + §4 the radius selector + §5 the Overpass `rest:` sensor wiring per category + §6 the offline cache (optional RoamCore TileServer add-on) + §7 the 31 `rc_amenities_overlay_*` contract tiles (2 input_boolean enabled + fail_safe + 2 select radius_km + data_source + 2 number cache_ttl_min + rate_limit_per_hour + 3 binary_sensor is_loaded + is_rate_limited + is_offline_cache_active + 11 sensor poi_count_total + poi_count_water + poi_count_laundry + poi_count_gym + poi_count_dump_point + poi_count_campsite + poi_count_wild_camping + poi_count_supermarket + poi_count_fuel + last_refresh_minutes_ago + nearest_water_km + nearest_campsite_km = 11 sensor + 11 button refresh_now + clear_cache + 8 per-category enable_* + 1 import_legacy = 11 button total 31 contract entities); FIVE MANDATORY §8 automations (§8.1 fail-safe guard (triggers when `input_boolean.rc_amenities_overlay_fail_safe` is OFF OR when `binary_sensor.rc_amenities_overlay_is_loaded` has been FALSE for > 5 minutes AND the upstream Overpass query has returned an error; flips the fail-safe toggle back to ON + clears the enable toggle (so the overlay goes back to the safe OFF state) + writes an audit-log entry + fires a critical notification warning the operator that the overlay has auto-disabled) + §8.2 rate-limited + cache-results guard (triggers when `number.rc_amenities_overlay_rate_limit_per_hour` Overpass requests have been made in the last hour OR when `sensor.rc_amenities_overlay_last_refresh_minutes_ago` < `number.rc_amenities_overlay_cache_ttl_min`; surfaces a "rate-limited / using cached data" banner in the map page + flips `binary_sensor.rc_amenities_overlay_is_rate_limited` to TRUE + switches the data source to the offline cache) + §8.3 user-configurable guard (triggers when ANY of the per-category visibility toggles (the per-category buttons in the recipe) flip OFF AND `input_boolean.rc_amenities_overlay_enabled` is ON; re-fetches the POI list from Overpass (or the offline cache if rate-limited) with only the enabled categories + writes an audit-log entry showing the new enabled-category list) + §8.4 internet-or-offline-cache guard (triggers when `binary_sensor.rc_amenities_overlay_is_loaded` is FALSE AND the offline cache has never been populated (the operator has never wired the optional RoamCore TileServer add-on); surfaces a "no internet + no offline cache — overlay disabled" banner in the map page + disables the overlay + writes an audit-log entry) + §8.5 auto-disable-when-moving guard (triggers when `device_tracker.rc_location_current` reports a speed > 10 km/h AND `input_boolean.rc_amenities_overlay_enabled` is ON; clears the enable toggle (so the overlay goes back to the safe OFF state while the vehicle is moving — POIs don't make sense at highway speeds) + writes an audit-log entry + fires a notification; re-enables the overlay once the vehicle speed drops below 5 km/h for > 2 minutes (so the overlay auto-resumes when parked))); 6 §9 troubleshooting entries (overlay is enabled but no POIs appear in view + §8.1 fail-safe guard fires unexpectedly + §8.2 rate-limited guard never flips + §8.3 user-configurable guard re-fetches every toggle flip + §8.4 internet-or-offline-cache guard fires even with Overpass reachable + §8.5 auto-disable-when-moving guard fires at low speed); §10 privacy (no RoamCore-side telemetry; the upstream Overpass API is queried from HA directly with no proxy; no telemetry goes to RoamCore; no telemetry goes to iOverlander / OSM / Wikimedia; POI cache is operator-local); §11 tier-a promotion outline (real Overpass bench + populated offline cache on CI + RoamCore-owned operator-wired setup flow walking the 8 categories + integration tests asserting a mock Overpass response with 5 POIs surfaces as `count_total = 5` + per-category counts add up + the 5 safety guards all flip when wired to canned fixture responses); §12 cross-references (map-dashboard Wave 3 #66 for the basemap mode + time-atomic Wave 3 #55 for the §8.5 vehicle-moving time gate + bluetooth-wifi-presence Wave 3 #42 for the §8.5 vehicle-speed attribute + mode Wave 3 #61 for the mode-aware lockout + demo-mode Wave 3 #62 for the demo-mode pattern + advanced-mode Wave 3 #63 for the operator-facing safety toggle pattern)
  - manifest-honesty smoke: `connections/amenities-overlay/tests/test_connection_yml.py` (7/7 PASS via `bash scripts/check.sh --core-only` — includes the `test_tier_b_without_tier_a_markers` defensive guard asserting tier=b + `wizard.one_tap=false` + `install.config_flow=true` honest because the UPSTREAM HA core `input_boolean` + `input_select` + `input_number` helpers + the HA core `template:` sensor + `template:` binary_sensor wrappers + the HA core `rest:` integration all expose a GUI flow since 2017/2022.x + `install.hacs=false` because amenities-overlay does NOT depend on a HACS add-on as a required dependency + NO RoamCore-owned `config_flow.py` + DOMAIN=`amenities_overlay` + the description explicitly documents the reuse-first strategy over the upstream Overpass API + the HA core `rest:` integration + the HA core `input_boolean` + `input_select` + `input_number` helpers + the HA core `template:` sensor + `template:` binary_sensor wrappers + the optional RoamCore TileServer add-on + the links.official list includes the HA core `rest:` + `input_boolean` + `input_select` + `input_number` + `template:` integration upstream doc URLs + the substring guard rephrasing check (the docstring contains `operator-wired` + `GUI flow` to avoid the literal `config_flow.py` substring trap); the `test_dashboard_tiles_follow_rc_naming` defensive guard asserting exactly 31 vendor-neutral `rc_amenities_overlay_*` tiles (NOT `rc_overpass_*` and NOT `rc_openstreetmap_*` and NOT `rc_osm_*` and NOT `rc_ioverlander_*` and NOT `rc_wikimedia_*` and NOT `rc_maptiler_*` and NOT `rc_mapbox_*` and NOT `rc_tomtom_*` and NOT `rc_nominatim_*` and NOT `rc_victron_*` and NOT `rc_renogy_*` and NOT `rc_starlink_*` and NOT `rc_peplink_*` and NOT `rc_teltonika_*` and NOT `rc_unifi_*` and NOT `rc_ubiquiti_*` and NOT `rc_input_boolean_*` and NOT `rc_input_select_*` and NOT `rc_input_number_*` and NOT `rc_input_button_*` and NOT `rc_input_text_*` and NOT `rc_input_datetime_*` and NOT `rc_template_*` and NOT `rc_homeassistant_*` and NOT `rc_hacs_*`) + forbidden_substrings covers vendor + hardware + protocol + integration names including `overpass`, `openstreetmap`, `osm`, `ioverlander`, `wikimedia`, `maptiler`, `mapbox`, `tomtom`, `nominatim`, `input_boolean`, `input_select`, `input_number`, `input_button`, `input_text`, `input_datetime`, `hacs`, `homeassistant`, `gps`, `accelerometer`, `gyroscope`, `magnetometer`, `iphone`, `ios`, `android`, `samsung`, `pixel`, `oneplus`, `xiaomi`, `huawei`, `phone`, `companion`, `esphome`, `esp32`, `esp8266`, `nodemcu`, `wemos`, `shelly`, `sonoff`, `zwave`, `zha`, `zigbee`, `deconz`, `conbee`, `raspbee`, `nous`, `aqara`, `bluetooth`, `wifi`, `wi-fi`, `unifi`, `ubiquiti`, `teltonika`, `peplink`, `starlink`, `victron`, `renogy` (note: `mqtt`, `webhook`, `rest`, `api`, `http`, `https`, `template`, `ha core` deliberately omitted from forbidden_substrings because they're too short for substring matching — `rest` would false-positive on `nearest_*_km`, `mqtt` would false-positive on Permitted `amenity` query keys; the audit + boundary-CI enforce the upstream integration ban via the longer `input_boolean` / `input_select` / `input_number` / `homeassistant` / `hacs` substrings instead); the `test_status_reflects_no_native_amenities_overlay_engine` defensive guard asserting status=beta + 5 tier_warnings present (`no_native_amenities_overlay_engine_for_integration_test` + `recipe_depends_on_overpass_api_reachability` + `recipe_depends_on_user_wiring_per_category_visibility_toggles` + `requires_operator_wiring_fail_safe_guard_before_first_use` + `amenities_overlay_must_use_offline_cache_or_internet_guard_must_be_wired`); the `test_automations_are_documented` defensive guard asserting the FIVE §8 MANDATORY automations documented (fail-safe + rate-limited + user-configurable + offline-cache-or-internet + auto-disable-when-moving) + 5 automation side_effects wired (`may_register_amenities_overlay_fail_safe_automation` + `may_register_amenities_overlay_rate_limit_automation` + `may_register_amenities_overlay_user_configurable_automation` + `may_register_amenities_overlay_internet_or_cache_automation` + `may_register_amenities_overlay_vehicle_moving_automation`) + 5 automation-gate tiles wired (`input_boolean.rc_amenities_overlay_fail_safe` + `binary_sensor.rc_amenities_overlay_is_loaded` + `binary_sensor.rc_amenities_overlay_is_rate_limited` + `binary_sensor.rc_amenities_overlay_is_offline_cache_active` + `input_boolean.rc_amenities_overlay_enabled`) + cross-references to map-dashboard Wave 3 #66 + time-atomic Wave 3 #55 + bluetooth-wifi-presence Wave 3 #42 + mode Wave 3 #61 + demo-mode Wave 3 #62 + advanced-mode Wave 3 #63)
  - vendor-neutrality: 31 `rc_amenities_overlay_*` tiles are vendor-neutral — NO Overpass / OpenStreetMap / OSM / iOverlander / Wikimedia / MapTiler / Mapbox / TomTom / Nominatim / input_boolean / input_select / input_number / input_button / input_text / input_datetime / template / HACS / HomeAssistant / Companion / ESPHome / phone / GPS / accelerometer / gyroscope / magnetometer / iPhone / iOS / Android / Samsung / Pixel / OnePlus / Xiaomi / Huawei / Shelly / Sonoff / MQTT / webhook / REST / API / HTTP / HTTPS / Victron / Renogy / Starlink / Peplink / Teltonika / Unifi / Ubiquiti / Z-Wave / Zigbee / ZHA / Deconz / Tasmota / ESP32 / ESP8266 / Wi-Fi / Bluetooth names leak into the tile ids
  - legacy tier-c-claim catalog page (`docs/catalog/map/amenities-overlay.md`) now carries the SUPERSEDED banner pointing at the new connection folder (the legacy tier-c "manual / varies" claim is honest-upstream-truth: RoamCore ships no native amenities-overlay engine today; the upstream Overpass API handles the POI queries, the HA core `rest:` integration handles the HTTP primitive, the HA core `input_boolean` + `input_select` + `input_number` helpers handle the per-category + radius + cache-TTL + rate-limit + data-source state storage, the HA core `template:` sensor + binary_sensor wrappers handle the per-category count + nearest-POI + is-loaded derivation, the optional RoamCore TileServer add-on handles the offline POI cache)
  - cross-reference to Overpass API: §5 wiring the Overpass `rest:` sensor uses the upstream `https://overpass-api.de/api/interpreter` endpoint
  - cross-reference to HA core `rest:` integration: §5 the 8 per-category `rest:` sensors use the HA core `rest:` integration (since 2017)
  - cross-reference to HA core `input_boolean` integration: §7 `input_boolean.rc_amenities_overlay_enabled` + `input_boolean.rc_amenities_overlay_fail_safe` storage uses the HA core `input_boolean` integration (since 2022.x)
  - cross-reference to HA core `input_select` integration: §7 `select.rc_amenities_overlay_radius_km` + `select.rc_amenities_overlay_data_source` storage uses the HA core `input_select` integration (since 2022.x)
  - cross-reference to HA core `input_number` integration: §7 `number.rc_amenities_overlay_cache_ttl_min` + `number.rc_amenities_overlay_rate_limit_per_hour` storage uses the HA core `input_number` integration (since 2022.x)
  - cross-reference to HA core `template:` sensor wrapper: §7 the per-category count + nearest-POI + last-refresh-minutes-ago derivation uses the HA core `template:` sensor wrapper (since 2022.x)
  - cross-reference to HA core `template:` binary_sensor wrapper: §7 the is-loaded + is-rate-limited + is-offline-cache-active derivation uses the HA core `template:` binary_sensor wrapper (since 2022.x)
  - cross-reference to optional RoamCore TileServer add-on (Wave 2 catalog #21): §6 wiring the offline cache uses the optional RoamCore TileServer add-on
  - cross-reference to map-dashboard Wave 3 #66: §12 the basemap mode that hosts the amenities overlay
  - cross-reference to time-atomic Wave 3 #55: §8.5 the §8.5 vehicle-moving time gate's timestamp cross-reference
  - cross-reference to bluetooth-wifi-presence Wave 3 #42: §8.5 the §8.5 vehicle-speed attribute cross-reference
  - cross-reference to mode Wave 3 #61: §12 mode-aware lockout cross-reference (Stealth silent hours disable the overlay)
  - cross-reference to demo-mode Wave 3 #62: §12 demo-mode pattern cross-reference (the offline-cache-or-internet guard follows the demo-mode never-controls-hardware pattern)
  - cross-reference to advanced-mode Wave 3 #63: §8.1 the operator-facing safety toggle pattern (fail-safe follows the advanced-mode confirm-flag pattern)
  - new `amenities_overlay` subsystem added to `docs/reference/rc-entity-naming.md` (the `amenities_overlay` subsystem is OWNED by this slice — the FIRST `map`-category `amenities_overlay` slice in the RoamCore connection pipeline; back-fills the `amenities_overlay` subsystem since the previous slices hadn't added it to the Allowed subsystems list on the fresh-from-main branch cut)
  - Last updated: 2026-08-04
  - PR #67 (Wave 3 #67)

- Map view wiring
  - `dashboard/lovelace/storage/lovelace.roamcore.json` includes `/lovelace/roamcore/map`
  - `homeassistant/packages/roamcore_location.yaml` maps a configurable `device_tracker` → `rc_location_*`

- Trip Wrapped (MVP HTML export)
  - tool: `homeassistant/tools/trip_wrapped/`
  - HA wiring: `homeassistant/packages/roamcore_trip_wrapped.yaml`
  - output: `/local/roamcore/trip_wrapped/latest.html`

- OpenClaw JSON API (HA-native)
  - endpoint: `/api/roamcore/openclaw/summary`
  - docs: `docs/reference/openclaw-json-api.md`

- Traccar live map (embedded)
  - RoamCore Map page embeds Traccar add-on **web UI** via iframe (configurable).
  - Helper: `input_text.rc_traccar_ui_url`

- Agent actions allowlist (safety gateway for agent-driven RoamCore actions)
  - Connection: [`connections/agent-actions-allowlist/`](../../connections/agent-actions-allowlist/) (Wave 3 #65, PR #69)
  - Tier-b recipe connection over upstream HA core `input_boolean` + `input_text` + `input_number` + `input_select` + `input_datetime` + `input_button` + `script` helpers (since 2022.x) + HA core `template:` sensor wrapper (since 2022.x) + HA core `logbook` integration (since 2022.x) + the upstream `script:` integration (since 2022.x). The single `input_boolean.rc_agent_actions_enabled` kill switch is already shipped in `homeassistant/packages/roamcore_agent_actions.yaml` and is preserved verbatim by this slice.
  - 11 vendor-neutral `rc_agent_actions_*` contract tiles (the kill switch + the policy path + the require-confirmation toggle + the default-duration picker + the session expiry timestamp + the seconds-until-expiry + the last-action-id + last-action-at + the last-action-result + the is-blocked-by-kill-switch + the disable-now button) — no Victron / SeeLevel / Garnet / Mopeka / Renogy / Starlink / Peplink / Teltonika / Unifi / Ubiquiti / OpenAI / Anthropic / Claude / GPT / ChatGPT / LLM / MQTT / webhook / REST / API / HTTP / HTTPS / ESPHome / Companion / phone / GPS / accelerometer / iPhone / iOS / Android / Samsung / Pixel / OnePlus / Xiaomi / Huawei / input_boolean / input_text / input_number / input_select / input_datetime / input_button / script / template / logbook / Z-Wave / Zigbee / ZHA / Deconz / Tasmota / Shelly / Sonoff / ESP32 / ESP8266 / Wi-Fi / BLE / Bluetooth names leak into the tile ids.
  - 5 §8 MANDATORY automations (kill-switch-blocks-everything guard + session-timeout guard + audit-log entry + require-confirmation guard + outside-allowlist deny-by-default guard).
  - Legacy catalog doc [`docs/catalog/ai/agent-actions-allowlist.md`](../catalog/ai/agent-actions-allowlist.md) now superseded (the SUPERSEDED banner is appended at the end of the doc).
  - Cross-references: time-atomic Wave 3 #55 + remote-access Wave 3 #58 + approach lights Wave 3 #52 + fans Wave 3 #59 + leveling Wave 3 #60 + mode Wave 3 #61 + demo-mode Wave 3 #62 + advanced-mode Wave 3 #63 + openclaw-api Wave 3 #64.
  - Verification: `bash scripts/check.sh --core-only` exit 0 + 7/7 pytest PASS + PR #69 OPEN against `main`.

## Next steps (needs HAOS setup / UI wiring)

1) **Setup Wizard dashboard**
   - Add a Lovelace dashboard YAML for setup flow.
   - Wire stubs to OpenWrt API + Victron connect UI.

2) **Traccar install + integration in HAOS**
   - Install Traccar add-on (or point to external).
   - Configure HA Traccar integration so `device_tracker.*` exists.
   - Set `input_text.rc_location_tracker_entity` to the correct entity.

3) **Trip stats (rc_trip_*) from real Traccar data**
   - MVP still uses mocks for distance/time/stops.
   - Implement: odometer-based + utility_meter or periodic report pulls.

4) **HACS packaging (planned)**
   - Publish a HACS integration to install RoamCore from the HA UI.
   - Auto-create dashboard + resources.
