# rootfs/

Filesystem overlay used for image/build workflows.

- `usr/` — files intended to be placed under `/usr` in a target system

If you’re looking for Home Assistant deployable files, see:
- `homeassistant/`
