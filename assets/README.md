# assets/

Repository-level static assets used by GitHub/Docs.

- `banner.png` — primary repo banner (used in README/docs where embedded)
- `banner1.png` — alternate/candidate banner

Keep this folder small; prefer `homeassistant/www/roamcore/` for assets that must be deployed to the HA box.
